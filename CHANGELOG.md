# Changelog
## [5.42.1] - 2026-09-15
### پشتیبانی هوشمند و جامع از متغیر WooID جهت جایگزینی خودکار شناسه ووکامرس در محصولات جدید و موجود
- **پشتیبانی کامل از شناسه `WooID` در شورت‌کد جدول مقایسه (`[buyruz_compare_table id="brz-ct-WooID"]`):** امکان استفاده از عبارت ثابت `WooID` در پرامپت‌ها و متون تولیدشده توسط هوش مصنوعی بدون نیاز به دانستن قبلی شناسه محصول.
- **جایگزینی خودکار در چرخه حیات ذخیره محصولات جدید و موجود:**
  - **محصولات موجود:** جایگزینی فوری عبارت `WooID` با شناسه واقعی محصول در فیلدهای محتوا (`post_content`) و چکیده (`post_excerpt`) پیش از ذخیره در دیتابیس (`wp_insert_post_data`).
  - **محصولات جدید:** شناسایی لحظه‌ای پس از ایجاد شناسه محصول در دیتابیس (`wp_after_insert_post`، `woocommerce_new_product` و هوک REST API) و اعمال خودکار شناسه واقعی در محتوا.
- **پشتیبانی در پل آفلاین (`BRZ_Offline_Bridge`):** جایگزینی خودکار `WooID` در هنگام دریافت و اعمال فیلدهای توضیحات و توضیحات کوتاه از بایروز شیت.
- **لایه محافظتی و رندر فرانت‌اند (Fail-Safe Resolution):** حل پویا و هوشمند `WooID` در هنگام اجرای شورت‌کد و فیلترهای نمایش محتوا (`the_content` و `woocommerce_short_description`) حتی اگر محصول بدون ذخیره مجدد باز شود.
- **پایداری کامل و پاس شدن ۱۰۰٪ آزمون‌های خودکار:** افزودن تست‌های واحد تخصصی در `CompareTableTest.php` با پاس شدن موفقیت‌آمیز تمامی ۴۰۴ آزمون با ۸۳۶ اعتبارسنجی.

## [5.42.0] - 2026-09-11
### یکپارچه‌سازی جامع بخش‌های مدیریت ویژگی‌ها در هاب مرکزی «ویژگی‌ها و فیلترها» و پشتیبانی کامل سایدبار از مشخصه‌های بایروز
- **یکپارچه‌سازی و تجمیع ۵ بخش مدیریتی در هاب مرکزی «ویژگی‌ها و فیلترها» (`buyruz-attributes-hub`):** ادغام ماژول‌های پراکنده پیشخوان وردپرس («چیدمان جدول مشخصات»، «فیلترهای سایدبار»، «مشخصات فیزیکی هسته»، «آنالیز مشخصات» و «خروجی مشخصات») درون یک هاب واحد، شیک، مینیمال و منطبق بر اصول روان‌شناسی کاربری.
- **نام‌گذاری کاملاً فارسی، کوتاه، رسا و تفکیک‌شده در ۵ تب اصلی هاب:**
  - 📋 **جدول محصول:** مدیریت چیدمان و نمایش ۳‌حالته مشخصات در جدول صفحه محصول (اجبار نمایش 🟢، اجبار مخفی 🔴، پیش‌فرض ⚪) با امکان کپی پرامپت هوش مصنوعی و جایگذاری سریع ساختار بهینه‌سازی شده.
  - 🔍 **فیلتر سایدبار:** مدیریت ترتیبی و فعال‌سازی فیلترهای سایدبار آرشیوها با قابلیت درگ و دراپ، بازسازی دستی کش و ابزارهای تنظیمات سایدبار.
  - ⚖️ **مشخصات فیزیکی:** مدیریت متمرکز و بهینه وزن، ابعاد، بارکد کالا (GTIN) و تنظیمات اسکیمای گوگل.
  - 🛠 **تعریف مشخصه‌ها:** سازنده فیلدهای متای اختصاصی بایروز با پشتیبانی از انواع متنی، عددی، بازه‌ای، بولین و آرایه‌ای.
  - 📊 **آنالیز و خروجی:** ارائه آمار دقیق فراوانی مشخصات در محصولات و استخراج فایل خروجی استاندارد JSON جهت مدل‌سازی داده و کارهای تحلیلی.
- **پشتیبانی کامل از تمامی انواع مشخصه‌های بایروز در فیلترهای سایدبار:** افزوده شدن کلیه مشخصات بایروز (`range`، `boolean`، `array`، `string`) به عنوان فیلترهای معتبر در چیدمان سایدبار و رندر هوشمند آنها (چک‌باکس‌های لایه‌ای باکالا برای انواع غیرعددی و اسلایدر بازه‌ای برای مقادیر عددی).
- **پایش و ریدایرکت خودکار پیوندهای قدیمی (`Zero Regression URL Routing`):** هدایت نرم و آنی تمام نشانی‌ها و بوک‌مارک‌های پیشین ماژول‌ها (`admin.php?page=buyruz-module-product_specs` و ...) به تب متناظر در هاب بدون هیچ‌گونه پیام خطای دسترسی.
- **انطباق کامل با پالت رنگ رسمی ۳-گانه بایروز:** طراحی تب‌ها، نشانگرها، دکمه‌های ناوبری و عناصر تعاملی با رنگ‌های سبز زمردی (`#05593D`)، سبز جنگلی (`#03432E`) و طلایی اکشن (`#FFB800`).
- **پایداری کامل و پاس شدن ۱۰۰٪ آزمون‌های خودکار:** اجرای موفقیت‌آمیز تمامی ۳۹۸ تست واحد PHPUnit با ۸۱۸ اعتبارسنجی بدون خطا.

## [5.41.0] - 2026-09-11
### پیاده‌سازی سیستم جامع فیلترهای هوشمند سایدبار و مدیریت چیدمان ۳‌حالته مشخصات محصول
- **ویجت جدید «فیلترهای هوشمند بایروز» (`BRZ_Widget_Smart_Filters`):** پیاده‌سازی ویجت مدرن و یکپارچه سایدبار منطبق ۱۰۰٪ با ساختار قالب باکالا، شامل پشتیبانی همزمان از مشخصات عددی بازه‌ای بایروز و ویژگی‌های تاکسونومی ووکامرس (`pa_*`).
- **محدودسازی هوشمند سایدبار به صفحات دسته‌بندی و آرشیو:** ویجت سایدبار صرفاً در آرشیوهای تاکسونومی (`is_product_taxonomy()` شامل دسته‌بندی‌ها، برچسب‌ها و برندها) فعال است و در برگه فروشگاه (`/shop/`) و صفحات جستجو به صورت خودکار پنهان می‌شود.
- **لایه کش پرسرعت ترنزینت دسته‌بندی‌ها (`brz_cat_filters_{term_id}`):** ذخیره داده‌های محاسباتی فیلترها و تعداد محصولات هر دسته‌بندی در ترنزینت ۲۴ ساعته با باطل‌سازی هوشمند و آبشاری (ترم و کلیه والدین سلسله‌مراتبی) در زمان ایجاد، ویرایش یا حذف محصولات.
- **سیستم مدیریت ۳‌حالته نمایش مشخصات در جدول صفحه محصول:** پشتیبانی از سه وضعیت هوشمند «اجبار نمایش» (🟢)، «اجبار مخفی» (🔴) و «پیش‌فرض» (⚪) برای کلیه مشخصات بایروز، مشخصات اصلی ووکامرس (وزن، ابعاد، GTIN) و ویژگی‌های استاندارد ووکامرس.
- **تب هاب چیدمان فیلترهای سایدبار (Layout Hub):** افزوده شدن امکان مرتب‌سازی ترتیبی فیلترها با درگ و دراپ (Drag & Drop)، سوئیچ فعال/غیرفعال سایدبار، و دکمه‌های کپی پرامپت بهینه‌سازی CRO برای ایجنت هوش مصنوعی و جایگذاری سریع خروجی.
- **نوار پویا و تعاملی فیلترهای فعال (Active Filters Badges):** تولید و نمایش داینامیک تگ‌های فیلترهای فعال در بالای لیست محصولات با دکمه‌های حذف آنی و پاک‌سازی همگانی (`Zero Hardcoded Modal/Component HTML`) بر پایه جاوااسکریپت خالص.
- **انطباق با پالت رنگ رسمی ۳-گانه بایروز:** بازطراحی بصری اسلایدرها، تگ‌ها، لودر، چک‌باکس‌ها و چیپس‌ها با رنگ‌های رسمی بایروز (`#05593D`، `#03432E` و `#FFB800`).
- **تضمین پایداری و پاس شدن ۱۰۰٪ آزمون‌های خودکار:** طراحی تست‌های واحد جدید در `SidebarSmartFiltersTest.php` با موفقیت کامل تمامی ۳۹۴ تست و ۷۹۳ اعتبارسنجی.

## [5.40.32] - 2026-09-10
### پشتیبانی کامل از ثبت و همگام‌سازی ویژگی‌های متای جدید (NewMetaProposals) در پل آفلاین
- **پشتیبانی از `specs` در ساختار `create_dependencies`:** امکان تعریف افزایشی و ثبت خودکار ویژگی‌های متای اختصاصی بایروز از طریق پل آفلاین و ذخیره بدون ریسک در تنظیمات فیلدهای متای سایت (`brz_product_specs_fields`).
- **پشتیبانی از اعتبارسنجی `add_specs` در کلاینت پل آفلاین:** اصلاح فایل `offline-bridge.js` جهت اعتبارسنجی بدون خطای دستورات افزودن مشخصات و نمایش داده‌های برگشتی `new_specs` در مودال برای همگام‌سازی آسان با گوگل شیت.
- **تضمین پایداری و پاس شدن ۱۰۰٪ تست‌های خودکار:** افزودن تست واحد جدید در `OfflineBridgeTest.php` با پاس شدن ۳۸۷ آزمون و ۷۶۷ اعتبارسنجی.

## [5.40.31] - 2026-09-08
### توقف کامل و تفکیک ارسال فوری تغییرات به پنل و سپردن تشخیص تغییرات به پایپلاین هوشمند کران‌جاب
- **خنثی‌سازی و توقف قطعی ارسال فوری رویدادها به پنل (`Decouple Fast-Dispatch`):** متدهای ارسال فوری به پنل (`dispatch_panel_build`) و ثبت فایل‌های صف (`queue/pending`) به طور کامل متوقف و خنثی شدند تا در هنگام ثبت سفارش‌های متعدد آنلاین، تغییرات انبار و قیمت مغازه، یا ویرایش مطالب توسط تیم محتوا، هیچ‌گونه درخواست پی‌درپی و غیرقابل کنترلی به سرور پنل ارسال نشود.
- **حفظ ۱۰۰٪ یکپارچگی هوک‌های سئو و سایت‌مپ:** تمامی متدهای به‌روزرسانی تاریخ اصلاح پست‌ها (`touch_post_modified`) و هماهنگی با سایت‌مپ‌های رسمی وردپرس و رنک‌مث حفظ شدند تا پایپلاین کران‌جاب جدید پنل بتواند به صورت متمرکز، هوشمند و بهینه در فواصل زمانی مشخص تغییرات را ارزیابی و بازسازی کند.
- **پاس شدن ۱۰۰٪ آزمون‌های خودکار:** به‌روزرسانی کلیه تست‌های واحد با موفقیت ۳۸۶ آزمون و ۷۶۳ اعتبارسنجی.

## [5.40.30] - 2026-09-08
### رفع ریشه‌ای دوپلیکیت شدن بیلدها در تغییر محصول/قیمت با مکانیزم تجمیع در چرخه درخواست و دی‌باونس ترنزینت
- **تجمیع نشانی‌های تحت تاثیر در طول چرخه درخواست HTTP وردپرس (`Request-Level Accumulation`):** در کلاس `BRZ_Static_Change_Trigger`، متغیر استاتیک `$request_touched_urls` اضافه شد تا تمامی هوک‌های پیاپی ووکامرس در هنگام ذخیره محصول (`woocommerce_product_set_regular_price`، `set_object_terms`، `save_post`) نشانی‌های خود را در طول چرخه درخواست تجمیع کنند و از ارسال چندین رویداد مجزا و پشت‌سرهم به صف پنل جلوگیری شود.
- **انتقال دیسپچ رویداد تغییرات به هوک پایانی `shutdown` وردپرس (`Single Atomic Shutdown Dispatch`):** با ثبت هوک `shutdown` با اولویت بالا (۹۹۹)، فرآیند دیسپچ و ایجاد رویداد ساخت در پنل دقیقاً یک‌بار در پایان کامل ذخیره‌سازی وردپرس اجرا می‌شود.
- **قفل و دی‌باونس ترنزینت بر اساس هش نشانی‌ها (`URL-Hash Debounce Lock`):** در متد `dispatch_panel_build`، بررسی ترنزینت با قفل زمانی ۲۰ ثانیه‌ای بر پایه هش نشانی‌های هدف اعمال گردید تا در صورت کلیک‌های مکرر یا درخواست‌های همزمان با نشانی‌های یکسان، از ساخت پکیج‌های تکراری و متوالی به طور کامل جلوگیری به عمل آید.
- **تضمین پایداری و پاس شدن ۱۰۰٪ آزمون‌های خودکار:** افزودن تست‌های واحد اعتبارسنجی دی‌باونس و تجمیع چرخه درخواست و پاس شدن موفقیت‌آمیز تمام ۳۸۶ آزمون واحد.

## [5.40.29] - 2026-09-08
### رفع ریشه‌ای باگ عدم شمولیت دسته‌بندی محصولات (نظیر بازی‌های پرونده‌ای)، پشتیبانی از هوک اختصاصی set_object_terms و پیمایش سلسلسه‌مراتبی دسته‌بندی‌ها
- **تغییر اولویت هوک `save_post` به ۲۵ جهت هماهنگی با ووکامرس (`Post-Metabox Hook Synchronization`):** در کلاس `BRZ_Static_Change_Trigger`، اولویت اجرای هوک `save_post` از ۱۰ به ۲۵ منتقل شد تا پس از ذخیره‌سازی قطعی متادیتاها و ترم‌های ووکامرس (که در اولویت ۲۰ اجرا می‌شوند) عمل کند و ترم‌های جدید از دست نروند.
- **شنود اختصاصی رویداد `set_object_terms`:** افزودن لیسنر اختصاصی برای هوک هسته وردپرس (`set_object_terms`) جهت استخراج و ارسال آنی دسته‌بندی‌ها و ترم‌ها به صف بازسازی هنگام ویرایش در پیشخوان، ویرایش سریع (Quick Edit)، ذخیره گروهی یا استفاده از ویرایشگر گوتنبرگ و REST API.
- **پاکسازی کش آبجکت ترم‌ها (`clean_object_term_cache`) و استعلام مستقیم از دیتابیس:** در متد `cascade_terms_change`، پیش از استعلام ترم‌ها کش آبجکت پست نامعتبر می‌شود و دسته‌بندی‌ها مستقیماً با `wp_get_object_terms` از دیتابیس دریافت می‌شوند تا داده‌های قدیمی در کش مانع از ثبت تغییرات نشوند.
- **بررسی مستقیم فیلدهای ارسالی فرم ادمین (`$_POST['tax_input']` و `$_POST['product_cat']`):** شناسایی بی‌درنگ دسته‌بندی‌های انتخاب‌شده در فرم ذخیره محصول حتی پیش از اتمام چرخه داخلی هسته وردپرس.
- **پیمایش سلسله‌مراتبی دسته‌های والد (`Hierarchical Category Traversal`):** با تغییر محصول در یک دسته فرزند (مانند `بازی‌های پرونده‌ای`)، علاوه بر خود دسته، دسته‌های والد آن (مانند `اسباب بازی فکری` و `اسباب بازی`) نیز به صورت خودکار به صف بازسازی اضافه می‌شوند تا ویترین دسته‌های بالاتر همواره با محصولات جدید به‌روز بماند.
- **ارسال پارامتر تاکسونومی در `get_term_link`:** اصلاح فراخوانی لینک دسته‌بندی با ارسال دقیق نام تاکسونومی جهت پیشگیری از خطای `WP_Error`.
### تضمین خودکار شمولیت صفحه اصلی فروشگاه و صفحه اصلی مجله در بازسازی تغییرات
- **افزودن خودکار صفحه اصلی فروشگاه در تغییرات فروشگاه (`Auto-Include Shop Home`):** در متد `dispatch_panel_build` کلاس `BRZ_Static_Change_Trigger`، بررسی دقیق بر روی نشانی‌های ارسالی رویداد تغییرات اعمال شد؛ چنانچه تغییری در محصولات، دسته‌بندی‌ها، برندها یا صفحات فروشگاه رخ دهد، نشانی صفحه اصلی فروشگاه (`/`) به صورت خودکار به بسته تغییرات افزوده می‌شود تا تغییرات در ویترین اصلی سایت نیز بلافاصله منعکس گردد.
- **افزودن خودکار صفحه اصلی مجله در تغییرات مجله (`Auto-Include Magazine Home`):** چنانچه تغییری در مقالات یا بخش‌های مجله رخ دهد، نشانی صفحه اصلی مجله (`/mag/`) به صورت خودکار به صف بازسازی افزوده می‌شود تا جدیدترین مقالات در صفحه اول مجله بی‌درنگ به‌روزرسانی شوند.
- **پاس شدن ۱۰۰٪ آزمون‌های خودکار:** افزایش تست‌های واحد خودکار به ۳۸۵ آزمون با پوشش کامل اعتبارسنجی شمولیت صفحات اصلی.

## [5.40.27] - 2026-09-08
### جلوگیری از بازسازی ناخواسته صفحه اصلی در آبشار تاکسونومی‌ها و فیلتر آدرس‌های تهی
- **پیشگیری قطعی از ارسال صفحه اصلی در تغییر قیمت محصول (`Root URL Cascading Guard`):** در متد `cascade_terms_change` کلاس `BRZ_Static_Change_Trigger`، اعتبارسنجی دقیق روی پیوندهای یکتا تاکسونومی‌ها اعمال شد تا در صورت بروز خطا در بازیابی لینک یا بازگشت آدرس اصلی سایت (`home_url('/')`)، این نشانی به عنوان تغییر ترم تلقی نشود و صفحه اصلی فروشگاه همراه با محصول بازسازی نگردد.
- **پالایش نشانی‌ها پیش از افزودن به صفحات انتخابی (`selected_pages`):** هرگونه نشانی با مسیر خالی، اسلش تکی یا خارج از ساختار مجاز ترم‌ها در متد آبشار نادیده گرفته می‌شود.
- **تضمین پایداری و پاس شدن ۱۰۰٪ تست‌های خودکار (Unit Tests):** کلیه ۳۸۴ آزمون خودکار افزونه با موفقیت کامل پاس شدند.

## [5.40.26] - 2026-09-08
### توقف ارسال ناخواسته کل صفحات سایت‌مپ به صف، تفکیک وظایف تولید نقشه و رفع همزمانی اجرای کران‌جاب
- **حذف دیسپچ خودسرانه کل صفحات سایت در متد تولید نقشه (`Pure Map Generation Separation`):** در متد `generate()` کلاس `BRZ_Static_Map_Generator`، ارسال خودکار صفحات دارای وضعیت معلق (`$merged_pending`) به صف پنل به طور کامل حذف شد. وظیفه این متد منحصراً بازتولید فایل نقشه `urls-map.json` است و ارسال بسته‌های ساخت به صف پنل صرفاً از طریق رویدادهای تغییر واقعی و اختصاصی (`BRZ_Static_Change_Trigger`) با نشانی‌های هدف مشخص انجام می‌شود تا دیگر هرگز کل ۱۰۶ صفحه سایت به اشتباه به صف بازسازی فرستاده نشوند.
- **رفع همپوشانی و اجرای دوگانه زمان‌بندی تغییرات (`Deduplicated Scheduler Trigger`):** در متد `schedule_regeneration`، ثبت هوک به صورت اولویت‌دار بازنویسی شد؛ در صورت وجود Action Scheduler ووکامرس، زمان‌بندی منحصراً به آن محول می‌شود و از ثبت مجدد و تکراری هوک در WP-Cron وردپرس جلوگیری شد تا رویدادها با فاصله چند ده ثانیه تکرار نشوند.
- **تضمین پایداری و پاس شدن ۱۰۰٪ تست‌های خودکار (Unit Tests):** کلیه ۳۸۴ آزمون واحد خودکار با موفقیت پاس شدند.

## [5.40.25] - 2026-09-08
### جداسازی و استعلام امن تاکسونومی‌ها (Fail-Safe Taxonomy Isolation) جهت تضمین ۱۰۰٪ ارسال دسته‌بندی‌ها، برچسب‌ها و برندها در رویدادهای تغییر قیمت
- **رفع قطعی علت عدم ارسال دسته‌بندی‌ها، تگ‌ها و برندها در تغییر قیمت (`Fail-Safe Per-Taxonomy Querying`):** در نسخه قبلی، ارسال همزمان آرایه‌ای از تاکسونومی‌ها به `wp_get_object_terms()` به دلیل عدم رجیستر بودن تاکسونومی‌های ناموجود (`pwb-brand` یا `brand` در هسته وردپرس)، منجر به پرتاب خطای درونی `WP_Error('invalid_taxonomy')` توسط هسته وردپرس و در نتیجه ابطال و خالی بازگشتن خروجی کل متد آبشار می‌شد. در این نسخه، استعلام تاکسونومی‌ها کاملاً مستقل، امن و تفکیک‌شده بازنویسی شد؛ تاکسونومی‌های ثبت‌شده به کمک `taxonomy_exists()` فیلتر شده و برای هر تاکسونومی استعلام جداگانه از طریق کش ترم‌های وردپرس (`get_the_terms()`) صورت می‌گیرد تا حتی در صورت نبود یک تاکسونومی فرعی، استخراج دسته‌بندی‌ها (`product_cat`)، برچسب‌ها (`product_tag`) و برندها (`product_brand`) با قطعیت ۱۰۰٪ انجام شود.
- **تزریق کامل و هماهنگ تمام تاکسونومی‌های مرتبط به بسته رویداد صف:** در زمان تغییر قیمت محصول، وضعیت موجودی یا ویرایش، اکنون تمام صفحات ترم‌های متصل به محصول (دسته‌بندی، تگ، برند) به صورت همزمان به نشانی محصول ضمیمه شده و در یک بسته رویداد به صف پنل ارسال می‌شوند تا پنل هم محصول و هم تمام آرشیوهای تاثیریافته (و صفحات شماره‌دار بعدی آن‌ها) را بازسازی کند.
- **پشتیبانی از تاکسونومی‌های برند در فرم جستجوی دستی ماژول کنترلر استاتیک:** اضافه شدن انواع تاکسونومی‌های برند به متد جستجوی کنترلر استاتیک در پیشخوان با فیلتر خودکار تاکسونومی‌های فعال.
- **افزایش پوشش آزمون‌های واحد خودکار (Unit Tests):** افزودن تست‌های تخصصی شبیه‌سازی تاکسونومی‌های نامعتبر، تضمین آبشار امن و آزمون ارسال محصول همراه با تمام ترم‌های مرتبط به صف پنل (۳۸۴ تست با موفقیت ۱۰۰٪).

## [5.40.24] - 2026-09-08
### شناسایی و ادغام کامل پست‌تایپ و تاکسونومی برند (product_brand)، آبشار آنی تغییرات قیمت به صفحات برند، دسته‌بندی و تگ‌ها
- **ارسال آبشاری و آنی نشانی‌های برند، دسته‌بندی و تگ در تغییرات قیمت و موجودی (`Cascaded Taxonomy Invalidation`):** بازطراحی متد `cascade_terms_change`؛ با تغییر قیمت محصول، وضعیت موجودی یا ویرایش، علاوه بر نشانی خود محصول، تمام صفحات آرشیو برند مرتبط (`product_brand`، `brand`، `pwb-brand`)، دسته‌بندی‌ها و برچسب‌های متصل به آن استخراج شده و به عنوان یک بسته واحد به صف بلادرنگ پنل ارسال می‌شوند تا با تغییر قیمت محصول، صفحات برند و دسته‌بندی بلافاصله در پنل به عنوان تغییریافته ثبت شوند.
- **ثبت خودکار صفحات برند و ترم‌ها در صفحات تحت‌نظر (`Auto-register Pending Terms`):** در صورت تغییر محصولی که برند یا دسته‌بندی آن پیش‌تر در جدول صفحات افزونه ذخیره نشده باشد، برگه آرشیو آن به صورت خودکار با وضعیت در انتظار (`pending`) به لیست صفحات افزوده می‌شود.
- **پشتیبانی کامل از تاکسونومی‌های برند در دیتکتور استاتیک:** هماهنگ‌سازی `BRZ_Static_Page_Detector` با تاکسونومی‌های `product_brand`، `pwb-brand` و `brand` جهت اعمال بهینه پروفایل‌های استاتیک‌ساز.
- **توسعه پوشش تست‌های خودکار (Unit Tests):** افزودن تست اختصاصی اعتبارسنجی آبشار برند و ثبت خودکار در `StaticChangeTriggerTest` با موفقیت ۱۰۰٪ (۳۸۲ تست).

## [5.40.23] - 2026-09-08
### استقلال کامل از کران‌جاب‌های خفته، ارسال بلادرنگ تغییرات قیمت و موجودی به صف پنل و پشتیبانی دوگانه از Action Scheduler
- **ارسال آنی و بدون تاخیر تغییرات به صف پنل (`Real-time Queue Fast-Dispatch`):** در کلیه رویدادهای تغییر قیمت (`on_price_change`)، تغییر موجودی (`on_stock_change`)، وضعیت موجودی (`on_stock_status_change`)، کسر موجودی خرید (`on_reduce_order_stock`)، ویرایش محصول و ذخیره متغیرها، نشانی صفحات تغییریافته بلافاصله در صف بلادرنگ پنل (`queue/pending/*.json`) درج شده و سیگنال وب‌رانر غیربلاک‌کننده ارسال می‌شود تا پنل ظرف ۰ ثانیه تغییرات را سنس کند.
- **استقلال کامل از ترافیک وب با ادغام Action Scheduler و بازخورد لوپ‌بک (`Action Scheduler & Loopback Resiliency`):** در متد `schedule_regeneration`، افزونه علاوه بر WP-Cron معمولی، از موتور قدرتمند Action Scheduler ووکامرس (`as_schedule_single_action`) و تریگر حلقه داخلی `spawn_cron()` استفاده می‌کند تا حتی در سایت‌های کاملاً استاتیک (بدون بازدیدکننده پویا روی وردپرس) یا کران‌جاب‌های روزانه سرور، پردازش تغییرات هرگز در خواب نماند.
- **پاکسازی صف و حذف رویدادهای خالی:** اعتبارسنجی دقیق نشانی‌ها پیش از نوشتن فایل در دایرکتوری صف جهت جلوگیری از ایجاد فایل‌های رویداد بدون نشانی.
- **پوشش تست‌های خودکار (Unit Tests):** افزودن آزمون‌های جامع دیسپچ فوری رویدادهای قیمت و زمان‌بندی پشتیبان در `StaticChangeTriggerTest` (۳۸۱ تست با موفقیت ۱۰۰٪).

## [5.40.22] - 2026-09-07
### تضمین بقای دائمی تنظیمات ماژول‌ها، رفع قطعی باگ خاموشی کنترلر استاتیک و تثبیت وضعیت صفحات ساخته‌شده
- **رفع قطعی باگ خاموش شدن خودکار ماژول کنترلر استاتیک در پیشخوان:** شناسایی و حذف حلقه مخرب میگریشن قدیمی در `class-brz-plugin.php` که در هر بار آپدیت افزونه، مقدار اسلاگ منسوخ‌شده `page_mapper` (با مقدار صفر) را روی `static_controller` بازنویسی می‌کرد و باعث خاموش شدن خودبه‌خودی ماژول می‌شد.
- **تضمین ۱۰۰٪ ثبات وضعیت ماژول‌ها و تنظیمات اختصاصی در تمامی آپدیت‌های بعدی:** پاکسازی اسلاگ‌های منسوخ‌شده قدیمی از پایگاه داده، خودترمیمی و روشن کردن خودکار ماژول کنترلر استاتیک در صورت خاموش بودن ناخواسته، و اضافه شدن پیش‌فرض مطمئن در `BRZ_Modules::get_states()` تا با آپدیت افزونه در هیچ‌کدام از دو سایت (فروشگاه یا مجله)، وضعیت روشن/خاموش ماژول‌ها یا هیچ تنظیمی تغییر نکند.
- **تثبیت دائم وضعیت صفحات ساخته‌شده (`done`) در نقشه صفحات (`merge_external_pages`):** اصلاح الگوریتم ادغام نقشه در `BRZ_Static_Map_Generator`؛ صفحات ساخته‌شده قبلی در صورت عدم تغییر محتوا یا تاریخ، وضعیت `done` خود را با قطعیت حفظ می‌کنند و از تبدیل غیرضروری تمام ۹۰ صفحه به حالت معلق (`pending`) پیشگیری شد.
- **حفظ ۱۰۰٪ صفحات خارجی (مجله و صفحات سفارشی) و تثبیت شمارش کل صفحات:** تضمین عدم حذف صفحات مجله (`/mag/...`) در هنگام بازسازی نقشه از سمت فروشگاه و بالعکس، و درج فیلدهای استاندارد نسخه قرارداد و شمارش کل در ریشه فایل نقشه جهت هماهنگی کامل با پنل.
- **پوشش تست‌های خودکار یکپارچگی (Unit Tests):** افزودن آزمون‌های جامع حفظ وضعیت صفحات ساخته‌شده، بقای صفحات مجله، و پیش‌فرض فعال بودن ماژول کنترلر در `StaticChangeTriggerTest` (۳۸۰ تست پاس‌شده).

## [5.40.21] - 2026-09-07
### ثبت ماژول کنترلر استاتیک در پیشخوان، دکمه‌های اقدام فوری در اعلان و فعال‌سازی پیش‌فرض بازسازی خودکار
- **ثبت رسمی ماژول کنترلر استاتیک در رجیستری ماژول‌های بایروز (`BRZ_Modules::registry`):** افزوده شدن `static_controller` به لیست ماژول‌های رسمی پیشخوان افزونه؛ فعال‌سازی نمایش کارت کنترلر استاتیک در داشبورد با امکان روشن/خاموش کردن، اضافه شدن زیرمنوی اختصاصی «کنترلر استاتیک» به سایدبار پیشخوان وردپرس، و رفع خطای دسترسی برگه هنگام کلیک روی دکمه مدیریت.
- **دکمه‌های اقدام آنی و یکپارچه در اعلان هشدار تغییرات صفحات (`Instant Action Buttons`):** بازطراحی اعلان «صفحات در انتظار بازسازی» با افزودن دو دکمه عملیاتی فوری:
  1. دکمه **«🚀 ارسال و بازسازی آنی در پنل»** جهت ارسال فوری و بدون معطلی صفحات در انتظار به صف پنل با فیدبک زنده و غیربلاک‌کننده (Zero Native Alert).
  2. دکمه **«⚡ فعال‌سازی بازسازی خودکار»** جهت روشن کردن دائم گزینه بازسازی خودکار با ۱ کلیک بدون نیاز به پیمایش به صفحه تنظیمات.
- **فعال‌سازی پیش‌فرض گزینه بازسازی خودکار هنگام تغییرات (`auto_regenerate_enabled = true`):** تنظیم مقدار پیش‌فرض بازسازی خودکار روی فعال تا کلیه تغییرات قیمت، موجودی یا محتوا بدون نیاز به تایید یا دخالت دستی، مستقیماً پس از پنجره دی‌باونس وارد چرخه ساخت استاتیک پنل شوند.
- **ارسال بلادرنگ رویداد به پنل در بازسازی دستی صفحات در انتظار (`generate_pending_only`):** فراخوانی مستقیم `dispatch_panel_build` در متد بازسازی دستی صفحات در انتظار تا علاوه بر به‌روزرسانی فایل نقشه روی سرور، رویداد فوری ساخت وب‌رانر نیز در پنل فعال شود.

## [5.40.20] - 2026-09-07
### بازطراحی لوکس جدول مقایسه محصول در موبایل، رفع باگ‌های قالب و ایزولاسیون استایل‌ها
- **بازطراحی ساختار موبایل بر پایه کارت‌های ارگونومیک (Refined Vertical Product Cards):** بازآفرینی کامل چیدمان جدول مقایسه در صفحات موبایل (`@media (max-width: 768px)`) به صورت کارت‌های مستقل و خوانا؛ اختصاص تمام عرض صفحه برای نمایش متون فارسی توصیفی (نظیر تمایز کلیدی و مشخصات مخاطب) با سطرچینی سخاوتمندانه (`line-height: 1.75`) و خوانایی بسیار بالا در انواع گوشی‌ها.
- **ایزولاسیون کامل در برابر باگ‌های ارتفاع قالب والد/چایلد (Theme Height Fix):** خنثی‌سازی کامل دستورات مخرب قالب باکالا (`height: 50px` روی `tr:first-child` و `height: 45px` روی `td`) که باعث پنهان شدن کل مشخصات محصول جاری و برش خوردن لبه پایینی حروف فارسی می‌شدند؛ تنظیم صریح `height: auto !important;` و `overflow: visible !important;` روی تمامی ردیف‌ها و خانه‌ها.
- **برجسته‌سازی فاخر محصول فعلی (Hero Card):** طراحی اختصاصی کارت محصول فعلی با حاشیه سبز رسمی بایروز (`#05593D`)، پس‌زمینه نعنایی ملایم (`#F0FDF4`) و نشان زرین و چشم‌نواز «محصول فعلی» (`#FFB800`) جهت جلب توجه مستقیم خریدار و لنگراندازی تصمیم خرید.
- **حذف کپسول تکراری عنوان در موبایل:** پنهان‌سازی هوشمند کپسول عنوان بالای جدول در موبایل جهت جلوگیری از تکرار نازیبا با تیتر اصلی بخش (`H2`) و آزادسازی فضای باارزش بالای صفحه برای شروع مستقیم کارت‌ها.
- **پوشش تست‌های خودکار (Unit Tests):** افزودن آزمون‌های یکپارچگی ساختار استایل موبایل در `CompareTableTest` و پاس شدن ۱۰۰٪ آزمون‌های سیستم (۳۷۸ تست بدون خطا).

## [5.40.19] - 2026-09-07
### معماری ضدتداخل همزمانی، بهینه‌سازی انقلابی تسویه‌حساب و سازگاری رسمی با HPOS ووکامرس
- **بهینه‌سازی انقلابی تسویه‌حساب مشتری (Zero Checkout Overhead):** پیاده‌سازی بافر هوشمند حافظه برای شناسه‌های کالا و دسته‌بندی‌ها؛ حذف کوئری‌های تکراری `UPDATE wp_options` و `UPDATE wp_termmeta` در زمان کسر موجودی خریدها و کاهش زمان پاسخگویی به زیر ۱۰ میلی‌ثانیه بدون هیچ‌گونه قفل‌شدگی سطر دیتابیس یا وقفه برای خریدار.
- **اعلام سازگاری رسمی با WooCommerce HPOS:** اتصال رسمی به هوک `before_woocommerce_init` و اعلام پشتیبانی استاندارد از High-Performance Order Storage (`custom_order_tables`) جهت رفع کامل اخطارهای بخش وضعیت ووکامرس.
- **قفل تراکنشی و فایل پشتیبان در نقشه صفحات (`urls-map.json.lock` & `.bak`):** اعمال قفل انحصاری تراکنشی در تمام چرخه‌های خواندن، ادغام و نوشتن نقشه مشترک فروشگاه و مجله جهت محو کامل پدیده شرایط مسابقه (Race Condition)؛ ذخیره خودکار فایل پشتیبان `.bak` پیش از هر جایگزینی و بازگردانی تضمین‌شده در صورت بروز هرگونه خرابی داده‌ها.
- **استانداردسازی نام‌گذاری رویدادهای صف:** تولید نام فایل رویدادها با فرمت استاندارد UTC با علامت `Z` و پسوند هگزادسیمال تصادفی جهت هماهنگی قطعی با اعتبارسنجی کران‌جان پنل.
- **ایمن‌سازی PHP 8.4 و پاکسازی سراسری Autoload:** جایگزینی تایپ‌هینت‌های شکننده، جلوگیری از ثبت متادیتای تکراری و اعمال بدون استثنای `autoload = 'no'` در تمامی متدهای ذخیره‌سازی گزینه‌های افزونه.

## [5.40.18] - 2026-09-07
### ادغام هوشمند نقشه دوگانه (فروشگاه و مجله) و بهینه‌سازی بار حافظه دیتابیس
- **ادغام هوشمند و حفظ صفحات متقابل (Domain-Aware Map Merge):** پیاده‌سازی متد `merge_external_pages` در کلاس `BRZ_Static_Map_Generator`؛ هنگام تولید نقشه صفحات (`urls-map.json`)، محتوای قبلی فایل روی دیسک بررسی شده و صفحات متعلق به سایت دیگر (آدرس‌های `/mag/` در صورت اجرا در فروشگاه، یا آدرس‌های ریشه فروشگاه در صورت اجرا در مجله) به طور قطعی حفظ می‌شوند تا از هرگونه بازنویسی متقابل یا حذف ناخواسته صفحات میان دو وردپرس جلوگیری به عمل آید.
- **بهینه‌سازی بارگذاری خودکار دیتابیس (`Autoload = No`):** غیرفعال‌سازی صریح فلگ Autoload برای گزینه حجیم `brz_options` در دیتابیس وردپرس جهت جلوگیری از لود غیرضروری چند مگابایت اطلاعات صفحات در درخواست‌های عادی و فرانت‌اند کاربران.

## [5.40.17] - 2026-09-07
### پوشش فراگیر و ۱۰۰٪ خودکار تغییرات دوگانه (فروشگاه و مجله) و همگام‌سازی بلادرنگ تاکسونومی‌ها
- **پشتیبانی مستقل و کامل از دو وردپرس مجزا (فروشگاه و وبلاگ):** راه‌اندازی و بوت‌استرپ سراسری کنترلر استاتیک در تمامی محیط‌های اجرای وردپرس (پیشخوان، فرانت‌اند، کران‌جاب، WP-CLI و REST API) هم برای سایت فروشگاه (`buyruz.com`) و هم سایت مجله (`buyruz.com/mag`) جهت شنیدن رویدادهای زنده کسر موجودی، دیدگاه‌ها و وب‌سرویس‌های انبارداری.
- **شنود مستقیم چرخه حیات دسته‌بندی‌ها و برچسب‌ها (Taxonomy Term Lifecycle):** اتصال به هوک‌های `created_term`، `edited_term` و `delete_term` برای تمام تاکسونومی‌های فروشگاه و وبلاگ (`product_cat`, `product_tag`, `product_brand`, `category`, `post_tag`) همراه با آپدیت متای زمان آخرین تغییرات (`_brz_last_modified`)، علامت‌گذاری وضعیت `pending` و پاکسازی خودکار ترم‌های حذف‌شده.
- **ثبت و بازسازی خودکار محتواهای کاملاً جدید:** اضافه شدن خودکار نوشته‌ها، برگه‌ها یا محصولات تازه‌منتشرشده به نقشه صفحات انتخابی و ارسال سریع به صف بیلد پنل بدون نیاز به انتظار برای سینک دوره‌ای سایت‌مپ.
- **تکمیل چرخه انتقال وضعیت پست‌ها:** ارتقای رهگیری تغییر وضعیت انتشار، زباله‌دان و پیش‌نویس برای انواع پست (`post`, `page`, `product`).

## [5.40.16] - 2026-09-07
### اتوماسیون کامل بازسازی استاتیک، ترک جامع موجودی/قیمت و دی‌بانس پویا
- **ترک جامع تغییرات موجودی و قیمت ووکامرس:** اتصال به کلیه هوک‌های حیاتی تغییر موجودی، وضعیت انبار، تغییرات محصولات متغیر و کاهش موجودی با ثبت سفارش (`woocommerce_product_set_stock`, `woocommerce_variation_set_stock`, `woocommerce_product_set_stock_status`, `woocommerce_product_stock_status_updated`, `woocommerce_reduce_order_stock`, `woocommerce_save_product_variation`)؛ پوشش کامل تغییرات ناشی از سفارش‌ها، ویرایش‌های دستی و وب‌سرویس‌ها / پل آفلاین حسابداری.
- **بازه زمانی تثبیت پویا و غیرهاردکد (Configurable Debounce Window):** اضافه شدن فیلد اختصاصی «تاخیر تثبیت تغییرات (ثانیه)» به تنظیمات استاتیک‌ساز بایروز با مقدار پیش‌فرض ۹۰ ثانیه (محدوده مجاز ۵ تا ۳۶۰۰ ثانیه) با امکان تغییر آزادانه توسط مدیر سایت، جهت تجمیع هوشمند تغییرات پشت‌سرهم و جلوگیری از اجرای بیلدهای غیرضروری.
- **دیسپچ آنی و غیربلاک‌کننده به پنل (Immediate Non-blocking Panel Dispatch):** ثبت صف تغییرات معلق به محض تولید نقشه تغییرات و ارسال فوری درخواست غیرهمگام به اسکریپت اجرایی وب پنل (`run-web.php`) بدون ایجاد وقفه یا تاخیر در تجربه کاربری وردپرس.

## [5.40.15] - 2026-09-06
### ارتقای ماژول AI Schema و رفع هشدارهای Merchant Listings سرچ کنسول گوگل
- **رفع هشدار `shippingDetails` (اطلاعات و هزینه حمل‌ونقل):** افزودن قابلیت تنظیمات هوشمند ارسال پستی در پیشخوان بایروز؛ تشخیص خودکار سفارش‌های بالای ۲ میلیون تومان و درج نرخ ارسال رایگان (۰ ریال) در اسکیما، به همراه ثبت هزینه پایه پست پیشتاز و آستانه رسمی ارسال رایگان (`freeShippingThreshold: 20000000 IRR`)، زمان‌های پردازش در انبار و ترانزیت پستی جهت دریافت بج رسمی ارسال رایگان در نتایج گوگل و پاسخ‌دهی دقیق موتورهای هوش مصنوعی (GEO).
- **رفع هشدار `hasMerchantReturnPolicy` (قوانین بازگشت کالا):** پیاده‌سازی کارت تنظیمات قوانین مرجوعی کالا منطبق با قانون تجارت الکترونیک (مهلت ۷ روزه کاری)؛ تمایز استاندارد میان نقص فنی/آسیب فیزیکی ارسال (`FreeReturn` بر عهده بایروز) و انصراف خریدار (`ReturnFeesCustomerResponsibility` بر عهده مشتری) همراه با شرط پلمپ بودن کالا (`NewCondition`).
- **معماری پویا و ضد هاردکد برگه‌ها:** اتصال دراپ‌داون برگه‌های وردپرس (`wp_dropdown_pages`) جهت انتخاب برگه رویه بازگرداندن کالا و استخراج خودکار پیوند داینامیک در اسکیما بدون وابستگی به اسلاگ ثابت.
- **رفع هشدار `validFrom` (اعتبار زمانی قیمت):** محاسبه و تکمیل خودکار تاریخ شروع اعتبار قیمت در بخش‌های `offers` و `priceSpecification` از روی تاریخ ایجاد محصول یا شروع فروش ویژه جهت تکمیل چرخه بازه اعتبار قیمت در گوگل.
- **پوشش تست‌های خودکار (Unit Tests):** افزودن کلاس تست واحد `AISchemaTest` با ۵ تست و ۲۹ بررسی مجزا و پاس شدن ۱۰۰٪ آزمون‌های سیستم.

## [5.40.14] - 2026-09-05
### پشتیبانی از ریدایرکت‌های وضعیت ۴۱۰ و ۴۵۱ در اندپوینت REST
- **اصلاح استخراج ریدایرکت‌های رنک‌مث:** اصلاح شرط فیلتر در اندپوینت `/wp-json/brz/v1/redirects` به طوری که ریدایرکت‌های با کدهای وضعیت `410` (محتوای حذف‌شده / Gone) و `451` (مسدودیت قانونی) که ذاتاً مقصد خالی دارند، حفظ شده و برای تبدیل به قوانین وب‌سرور Nginx تحویل پنل استاتیک‌ساز داده شوند.

## [5.40.13] - 2026-09-04
### ماژول اختصاصی صوت و پادکست معرفی محصول (Product Audio Player)
- **متاباکس اختصاصی در صفحه ویرایش محصول ووکامرس:** امکان ثبت لینک مستقیم فایل صوتی (آروان کلود یا هاست دانلود)، تعیین عنوان سفارشی و انتخاب محل نمایش در صفحه محصول بدون نیاز به دستکاری کدهای HTML.
- **جای‌گیری هوشمند و وسط‌چین در صفحه محصول:** نمایش پلیر به صورت کاملاً وسط‌چین با پالت ۳ رنگ رسمی بایروز زیر توضیحات کوتاه (کنار گالری)، قبل از دکمه افزودن به سبد، یا درون تب مجزای ووکامرس.
- **سئو و هوش مصنوعی مولد (GEO):** تزریق خودکار اسکیمای استاندارد `AudioObject` به گراف رنک‌مث پرو (Rank Math SEO PRO) با برچسب زبان فارسی (`fa-IR`) جهت ثبت ریچ‌اسنیپت صوتی در نتایج گوگل.
- **شورت‌کد مستقل `[brz_audio]`:** امکان فراخوانی پلیر استاندارد در تمامی بخش‌های سایت و نوشته‌ها.

## [5.40.12] - 2026-08-31
### ممیزی نهایی و یکپارچگی ماژول‌ها
- **ارتقای سازگاری:** هماهنگ‌سازی ماژول‌های فرانت‌اند با نسخه ۵.۱۰۱.۲۰ پنل.

## [5.40.11] - 2026-08-31
### هماهنگ‌سازی ماژول‌های فرانت‌اند و کش CDN
- **بهبود یکپارچگی استقرار:** انطباق ساختار فراخوانی اسکریپت‌ها با نسخه ۵.۱۰۱.۱۹ پنل.

## [5.40.10] - 2026-08-31
### هماهنگ‌سازی روت‌های استاتیک و ریدایرکت‌ها
- **ارتقای یکپارچگی:** همگام‌سازی قوانین دسترسی به فایل‌های استاتیک با پنل نسخه ۵.۱۰۱.۱۸.

## [5.40.9] - 2026-08-31
### هماهنگ‌سازی ماژول‌ها و استقرار سرور
- **بهبود یکپارچگی استقرار:** انطباق ساختار خروجی و ارتباط افزونه با نسخه ۵.۱۰۱.۱۷ پنل.

## [5.40.8] - 2026-08-31
### یکپارچگی استانداردهای بهینه‌سازی لود و سیستم کش تصاویر
- **بهبود سازگاری افزونه:** ارتقای هماهنگی لود تنبل و ارسال متادیتاها با پنل استاتیک‌ساز نسخه ۵.۱۰۱.۱۶.

## [5.40.7] - 2026-08-30
### بهینه‌سازی بارگذاری تنبل تصاویر و سازگاری با سیستم کش و تصاویر استاتیک
- **بهبود سازگاری افزونه با توابع بارگذاری تصاویر در آرشیوها:** هماهنگ‌سازی ساختار لود عکس‌ها با موتور استاتیک‌ساز.

## [5.40.6] - 2026-08-30
### هماهنگ‌سازی ارتباط با پنل استاتیک‌ساز و ارسال وضعیت
- **بهبود یکپارچگی ضربان قلب و مانیتورینگ:** همگام‌سازی ارتباطات وضعیتی بین افزونه و پنل متمرکز.

## [5.40.5] - 2026-08-30
### تضمین یکپارچگی ساختار المان‌های فرانت‌اند و گرید محصولات
- **بهینه‌سازی خروجی دسته‌بندی‌ها:** هماهنگی المان‌های ووکامرس برای حفظ استایل‌ها و گرید ۳ ستونه هنگام استفاده از استاتیک‌ساز.

## [5.40.4] - 2026-08-30
### هماهنگ‌سازی و یکپارچه‌سازی متد رهگیری استاتیک بارگذاری محصولات بیشتر در دسته‌بندی‌ها
- **ارتقای سازگاری با موتور کلاینت-ساید بایروز:** بهینه‌سازی ساختار لینک‌های صفحه‌بندی آرشیوها جهت تعامل روان با موتور رهگیری Capture استاتیک‌ساز و حذف هرگونه وابستگی به درخواست‌های AJAX سرور مبدا.

## [5.40.3] - 2026-08-30
### ارتقای پایداری استخراج ساختار و جلوگیری از خطاهای توابع هسته وردپرس (`Core Function Fallbacks & Robust Map Generation`)
- **پایداری توابع هسته در استخراج نقشه آدرس‌ها (`BRZ_Static_Map_Generator`):** بررسی امن وجود تابع `get_term` قبل از فراخوانی جهت جلوگیری از بروز خطای فیتال در محیط‌های تست و سناریوهای بارگذاری زودهنگام کنترلر استاتیک.

## [5.40.2] - 2026-08-29
### قفل رندرینگ تک‌باره (Static Render Guard) جهت پیشگیری قطعی از تکرار باکس نویسنده
- **جلوگیری از چاپ تکراری باکس نویسنده در صفحه مقاله:** اضافه شدن فلگ استاتیک ضدتکرار (`$author_rendered`) به متد `render_author_box` تا در صورت اجرای همزمان فیلتر `the_content` و هوک قالب، باکس نویسنده دقیقاً یک‌بار در انتهای متن مقاله ظاهر شود.
- **بهبود گارد سربرگ متادیتا و مسیر راهنما:** تضمین رندرینگ تک‌باره برای سربرگ متادیتا و بردکرامب در تمام سناریوهای قالب.

## [5.40.1] - 2026-08-29
### اتصال هوشمند سربرگ متادیتا به هوک زیر عنوان و تفکیک مسیر راهنما طبق استاندارد مجلات مدرن
- **اتصال نوار متادیتا به هوک اختصاصی `generate_after_entry_title`:** قرارگیری دقیق و زیبای نوار متادیتا (عکس نویسنده، تاریخ، زمان مطالعه) بلافاصله زیر تیتر H1 و قبل از تصویر شاخص بدون ایجاد تکرار در متن مقاله.
- **انتقال مسیر راهنما (Breadcrumbs) به بالای تیتر اصلی (`generate_before_entry_title`):** چینش ساختاریافته به ترتیب: مسیر راهنما ➔ تیتر اصلی ➔ نوار متادیتا ➔ تصویر شاخص ➔ متن مقاله ➔ باکس نویسنده.
- **بهبود رفتار تزریق خودکار:** پیشگیری هوشمند از تکرار سربرگ متادیتا در متن با بررسی وضعیت اجرای اکشن‌های قالب.

## [5.40.0] - 2026-08-29
### ماژول جامع تحریریه هوشمند، سربرگ متادیتای مقالات، باکس نویسنده چندنقشی و آواتار محلی بومی (`BRZ Editorial Suite`)
- **ماژول مستقل و یکپارچه تحریریه بایروز (`BRZ_Editorial`):** طراحی و پیاده‌سازی ماژول سبک و مدرن تحریریه بدون وابستگی به قالب، با قابلیت تزریق خودکار و شورت‌کدهای `[brz_article_hero]` و `[brz_author_box]` جهت جایگزینی قطعی المان‌های قالب و صفحه‌سازها.
- **سیستم چندنقشی هوشمند در صفحه ویرایش نوشته:** افزودن متاباکس اختصاصی با امکان انتخاب ۴ حالت محتوایی شامل «✍️ تألیف و نگارش اختصاصی»، «🌐 ترجمه و بازنویسی تخصصی»، «🩺 بازبینی و تأیید علمی» و «👤 یادداشت مهمان / کارشناس مدعو» به همراه فیلدهای منبع اصلی، پیوند و تنظیمات Follow/Nofollow.
- **سربرگ متادیتای بومی بالای نوشته (Top Page Hero):** نمایش زیبا و بهینه آواتار محلی نویسنده/مترجم، پیشوند نقش، تاریخ شمسی، نشان زمان مطالعه و مسیر راهنما (Breadcrumbs رنک‌مث) در ابتدای مقالات.
- **باکس نویسنده سبک دیجیاتو/دیجی‌کالا مگ با ساختار Mobile-First:** طراحی کارت مدرن با پالت رسمی سبز بایروز، نشان تایید رسمی تحریریه (Verified Badge)، وزن فونت نرمال و خوانا، نوار استناد به منبع در مقالات ترجمه‌ای و دکمه اختصاصی «مشاهده کلیه مقالات نویسنده» با شمارنده نوشته‌ها.
- **موتور آواتار محلی بومی و حذف وابستگی به گراواتار (`BRZ_Local_Avatar`):** افزودن دکمه انتخاب تصویر از کتابخانه رسانه وردپرس و فیلد سمت تخصصی در شناسنامه کاربر، قطع ۱۰۰٪ ارتباط با گراواتار خارجی و امکان حذف دائمی افزونه‌های جانبی مانند Simple Local Avatars.
- **یکپارچگی عمیق با Rank Math SEO PRO و داده‌های ساختاریافته (`BRZ_Rank_Math_Bridge`):** غنی‌سازی مستقیم گراف اسکیما رنک‌مث با فیلدهای استاندارد `translator`، `isBasedOn`، `reviewedBy` و `jobTitle` بر اساس استانداردهای Schema.org و Google E-E-A-T بدون ایجاد اسکیماهای تکراری.
- **عملکرد و سرعت حداکثری:** بدون حتی ۱ بایت جاوااسکریپت در فرانت‌اند، حجم استایل زیر ۲ کیلوبایت، بدون پرش چیدمان (CLS = 0.000) و سازگاری کامل با کش لایت‌اسپید.
### تولید و کشف خودکار و جامع تمام صفحات شماره‌دار آرشیو و دسته‌بندی‌ها در نقشه استاتیک (`Automated Full Pagination URL Harvesting for Static Engine`)
- **تولید خودکار آدرس صفحات ۲ به بعد برای دسته‌بندی‌ها و برچسب‌ها (`BRZ_Static_Map_Generator`):** محاسبه خودکار تعداد صفحات هر دسته‌بندی بر اساس تعداد محصولات/نوشته‌ها و تنظیمات تعداد در صفحه ووکامرس/وردپرس، و درج مستقیم تمام آدرس‌های صفحات شماره‌دار (`/page/2/`، `/page/3/` تا آخرین صفحه) در فایل `urls-map.json`.
- **پشتیبانی از صفحه‌بندی برگه اصلی فروشگاه و وبلاگ:** محاسبه و ثبت خودکار صفحات آرشیو فروشگاه (`shop`) و وبلاگ در صورت وجود صفحه‌بندی بیش از ۱ صفحه.
- **تضمین ۱۰۰٪ در دسترس بودن داده‌ها برای لود ایجکس و سئو:** تضمین ایجاد فیزیکی فایل‌های تمام صفحات آرشیو در بیلد استاتیک جهت اجرای روان قابلیت «بارگذاری بیشتر / اسکرول بی‌نهایت» قالب باکالا بدون خطای ۴۰۴.

## [5.39.0] - 2026-08-26
### ماژول جامع مانیفست وب‌اپلیکیشن (PWA Web Manifest) و فاوآیکون استاندارد ریشه (Root Assets Suite)
- **ماژول اختصاصی مانیفست PWA (`BRZ_Web_Manifest`):** طراحی و پیاده‌سازی ماژول جدید استاندارد W3C با امکان فعال/غیرفعال‌سازی در پیشخوان، پشتیبانی کامل از PWA، قابلیت Add to Home Screen، نام کامل و نام کوتاه فارسی، و بهینه‌سازی شده برای موبایل و دسکتاپ.
- **همگام‌سازی فیزیکی دارایی‌های ریشه (`manifest.json` و `favicon.ico`):** استخراج خودکار نمادک سایت و تولید فایل‌های فیزیکی سبک در ریشه وردپرس (`ABSPATH`) جهت انتقال ۱۰۰٪ خودکار توسط پنل استاتیک‌ساز و سبز شدن کامل ماتریس سلامت دارایی‌های ریشه.
- **تزریق بهینه متاتگ‌های هدر و PWA (`wp_head`):** تزریق استاندارد تگ‌های مانیفست، رنگ تم سازمانی (`theme-color`)، متاتگ‌های وب‌اپلیکیشن اپل (`apple-mobile-web-app-*`) و آیکون بدون کمترین سربار دیتابیس یا افت سرعت در لود صفحات.
- **استریم داینامیک سریع و کش بهینه:** ارائه اندپوینت با سرعت پاسخ‌دهی زیر ۵ میلی‌ثانیه برای درخواست‌های مانیفست با هدرهای استاندارد CORS و Cache-Control.
- **پنل مدیریت پیشرفته با رابط کاربری مدرن:** دارای ویجت وضعیت لحظه‌ای فایل‌های ریشه، انتخاب‌گر رنگ تم، دکمه بازسازی مجدد و سیستم اعلان غیرمسدودکننده توست.

## [5.38.1] - 2026-08-25
### همسان‌سازی کامل المان اسلایدر مقالات مجله بایروز با اسلایدر پست‌های باکالا (Bakala Posts Slider Mirroring)
- **تغییر آیکون المان به آیکون اسلایدر پست‌ها:** جایگزینی آیکون ویجت با `eicon-code-highlight` (کارت‌های هم‌پوشان) جهت انطباق ظاهری ۱۰۰٪ با المان «اسلایدر پست ها» در دسته‌بندی «المان‌های وبلاگ باکالا» در المنتور.
- **بازنویسی تب‌ها و کنترل‌های المنتور مطابق باکالا:** پیاده‌سازی ۳ بخش کنترلی استاندارد شامل «کنترلر المان» (عنوان، تعداد مقالات، فیلتر دسته، مرتب‌سازی صعودی/نزولی، نمایش بر اساس جدیدترین/تصادفی، سوییچ زمان مطالعه، دکمه و لینک بیشتر، تگ عنوان)، «تنظیمات اسلایدر» (تعداد در دسکتاپ/تبلت/موبایل، ناوبری، چرخش بی‌نهایت، حرکت خودکار و سرعت) و «تنظیمات ظاهری» (انتخاب استایل ۱ و ۲ و ۳، رنگ عنوان، رنگ آیکون نوار، رنگ عنوان مقالات و پس‌زمینه).
- **پشتیبانی کامل از هر ۳ استایل با ساختار نیتیو باکالا:** رندر مستقیم ساختار HTML و کلاس‌های باکالا (`pro_carousel`, `style-three-wrapper`, `carousel_post_style_three`, `carousel_post_style_two`, `carousel_post_one`) همراه با اسلایدر Slick و محاسبه تقویم جلالی برای استایل دوم.
- **سازگاری کامل رو به عقب (Backward Compatibility):** حفظ هماهنگی با تنظیمات و ویجت‌های ذخیره‌شده از قبل.

## [5.38.0] - 2026-08-25
### معماری ماژولار جامع و پشتیبانی هوشمند از مجله خبری بایروز (Universal Magazine & Shop Architecture)
- **موتور تشخیص هوشمند و مدیریت پروفایل سایت (Site Profile Engine):** پیاده‌سازی کلاس `BRZ_Profile` با قابلیت تشخیص خودکار و سوئیچ دستی بین حالت «فروشگاه بایروز» و «مجله خبری بایروز»؛ در حالت مجله که ووکامرس غیرفعال است، تمامی ماژول‌های ووکامرسی بدون ایجاد خطا یا مصرف رم کنار گذاشته می‌شوند.
- **پل ارتباطی مستقیم و پرسرعت درون‌سروری (Cross-Site Bridge & REST API):** پیاده‌سازی سیستم تبادل خودکار داده‌های مقالات و محصولات بین دو دیتابیس با اندپوینت‌های امن `/wp-json/buyruz/v1/mag-posts` و `/wp-json/buyruz/v1/shop-products` همراه با کش هوشمند `Transient` وردپرس و پاکسازی خودکار هنگام بروزرسانی مطالب.
- **تزریق خودکار مقالات مجله به اسلایدر باکالا در صفحه اصلی فروشگاه (Bakala Bridge):** اتصال به فیلتر `posts_pre_query` جهت نمایش مستقیم و بدون تاخیر مقالات مجله خبری در المان‌های اسلایدر مقالات قالب باکالا در صفحه اصلی فروشگاه بدون دستکاری سورس قالب.
- **ویجت اختصاصی المنتور برای اسلایدر مقالات مجله (BRZ Mag Carousel Widget):** طراحی ویجت کامل المنتور با پشتیبانی از استایل شاخص باکالا (پست بزرگ و لیست اسکرول)، گرید چندستونه و استایل فشرده همراه با فیلتر دسته‌بندی و نشان زمان مطالعه.
- **بلوک نیتیو گوتنبرگ و شورت‌کد معرفی محصولات در مجله (Buyruz Product Showcase):** طراحی بلوک مدرن گوتنبرگ `buyruz/product-showcase` و شورت‌کد `[brz_shop_products]` با قابلیت فیلتر بر اساس دسته، برچسب، شناسه و تخفیف‌دارها متناسب با قالب GeneratePress.
- **ماژول ابزارهای تخصصی مجله خبری (Magazine Tools):** محاسبه دقیق زمان تخمینی مطالعه متون فارسی (۲۰۰ کلمه در دقیقه با پشتیبانی از اعراب)، شورت‌کد `[brz_reading_time]`، فهرست خودکار سرفصل‌های مقاله `[brz_toc]` و ستون اختصاصی در پیشخوان نوشته‌ها.

## [5.37.2] - 2026-08-25
### سیستم ره‌گیری بلادرنگ و خودکار دیدگاه‌ها، انتشار آبشاری دسته‌بندی‌ها و ویرایش تصاویر (Zero-Discrepancy Change Tracking)
- **ره‌گیری خودکار دیدگاه‌ها و نظرات کاربران (Auto-Touch on Comments):** اتصال به کلیه رویدادهای ثبت، تایید، ویرایش، حذف و بازیابی دیدگاه‌ها در وردپرس (`comment_post`، `wp_set_comment_status`، `edit_comment` و...) و بروزرسانی لحظه‌ای `post_modified_gmt` نوشته و محصول والد جهت آپدیت فوری تگ `<lastmod>` در نقشه سایت و شناسایی ۱۰۰٪ قطعی در حالت «ساخت تغییرات» استاتیک‌ساز.
- **انتشار آبشاری تغییرات به دسته‌بندی‌ها و برچسب‌های والد (Parent Term Cascade):** با ذخیره یا بروزرسانی هر محصول یا مقاله، تمامی دسته‌بندی‌ها، برچسب‌ها و برندهای متصل به آن استخراج شده و ضمن بروزرسانی متای زمان تغییر ترم (`_brz_last_modified`)، وضعیت صفحات آرشیو مربوطه در نگاشت استاتیک به `pending` تغییر می‌یابد تا صفحات آرشیو همواره همگام با محصولات جدید بازسازی شوند.
- **ره‌گیری خودکار ویرایش و تعویض فایل‌های رسانه (Media/Attachment Tracking):** اتصال به رویدادهای ویرایش و حذف پیوست‌ها (`edit_attachment`، `attachment_updated`، `delete_attachment`) و بروزرسانی خودکار تاریخ تغییر پست‌های والد، تصاویر شاخص و گالری تصاویر محصول.
- **پشتیبانی کامل از بروزرسانی‌های ووکامرس (REST API / Bridge Hooks):** اتصال به هوک `woocommerce_update_product` جهت ره‌گیری تغییرات قیمت و موجودی از طریق کلیه سرویس‌ها، پل‌های آفلاین و ایپی‌آی‌های ترب و حسابداری.

## [5.37.1] - 2026-08-19
### افزودن اندپوینت REST اکسپورت ریدایرکت‌های فعال رنک‌مث برای استاتیک‌ساز (`/wp-json/brz/v1/redirects`)
- **اندپوینت امن استخراج ریدایرکت‌ها (`GET /wp-json/brz/v1/redirects`):** استخراج تمامی ریدایرکت‌های فعال ۳۰۱ و ۳۰۲ ثبت‌شده در افزونه Rank Math SEO جهت تبدیل خودکار به رول‌های Nginx و انتقال یکپارچه به سرور لبه استاتیک.
- **به‌روزرسانی ساختار امنیتی و هشدار مشارکت:** بازبینی و تثبیت هشدارهای فایل‌ها و انطباق با قوانین توسعه.

## [5.37.0] - 2026-08-18
### ریشه‌کن‌سازی پاپ‌آپ‌های مسدودکننده و پیاده‌سازی سیستم اعلان مدرن توست (Zero Alert Policy & Toast Notifications)
- **حذف دائمی تمام alert و confirm بومی مرورگر:** جایگزینی کامل تمامی پاپ‌آپ‌های مسدودکننده مرورگر در ماژول‌های لینک‌ساز هوشمند، ویژگی‌های محصول، فیلترهای کناری، اتصالات و بهینه‌ساز LLMS با اعلان‌های مدرن اسنک‌بار/توست غیرمسدودکننده.
- **ثبت قانون دائمی عدم استفاده از alert:** درج قانون الزامی و سخت‌گیرانه در مانیفست‌ها و دستورالعمل‌های ایجنت برای جلوگیری قطعی از اضافه شدن مجدد هرگونه alert به کدهای پروژه.

## [5.36.3] - 2026-08-16
### پشتیبانی همه‌جانبه از تمام انواع پست‌ها و دیکود اسلاگ‌ها در اندپوینت عناوین
- **پوشش کلیه پست‌تایپ‌های فروشگاه و مجله:** استخراج تمامی محصولات، برگه‌ها، مقالات، انواع پست‌های سفارشی و دسته‌بندی‌ها به صورت فارسی روان با پشتیبانی از اسلاگ‌های فارسی و انگلیسی دیکود شده.

## [5.36.2] - 2026-08-16
### ارائه اندپوینت REST عناوین فارسی محصولات و صفحات برای پنل متمرکز (`/buyruz/v1/titles`)
- **اندپوینت اختصاصی استخراج عناوین (`GET /wp-json/buyruz/v1/titles`):** استخراج سریع و بلادرنگ نام تمامی محصولات قالب باکالا، برگه‌ها، مقالات وبلاگ و دسته‌بندی‌ها به زبان فارسی روان جهت نمایش دقیق و جستجوی دوزبانه در پنل عملیات استاتیک‌ساز بایروز.

## [5.36.1] - 2026-08-14
### تثبیت کامل ابعاد گالری محصول موبایل و حذف نهایی خطای CLS
- **رزرو لحظه‌ای ابعاد گالری تصویر محصول موبایل (Critical Aspect-Ratio Reservation):** اعمال نسبت ابعاد ۱:۱ (`aspect-ratio: 1 / 1`) برای اسلایدر، کانتینرها و تصاویر گالری محصول در تگ `<head>` تا قبل از بارگذاری کامل تصویر یا اجرای Swiper، کانتینر در ابعاد دقیق رزرو شود و از جابجایی محتوای صفحه در موبایل به صفر مطلق برسد.
- **تست و اعتبارسنجی ۱۰۰٪ سبز:** رفع قطعی خطای Cumulative Layout Shift در نمای موبایل و دسکتاپ.

## [5.36.0] - 2026-08-14
### رفع خطای لینک a#scrollUp و بهینه‌سازی پرش چیدمان (Zero CLS Elimination)
- **پوشش دکمه اسکرول به بالا (`a#scrollUp`):** تزریق خودکار `aria-label="بازگشت به بالای صفحه"` و `title` به دکمه بازگشت به بالای صفحه در هر دو لایه SSR و رویدادهای کلاینت جاوااسکریپت.
- **صفر کردن پرش چیدمان (CLS = 0):** حذف دستکاری ابعاد با جاوااسکریپت پس از لود صفحه و تزریق استایل‌های بحرانی در هدر HTML (`<head>`) جهت رندر نقد و بررسی بدون پرش و در لحظه اول بارگذاری.

## [5.35.1] - 2026-08-14
### ارتقای سیستم هوشمند چتر نجات لینک‌های دسترس‌پذیر (Universal Accessible Links Engine)
- **پوشش ۱۰۰٪ تمام لینک‌های آیکونی گالری و ابزارهای محصول موبایل:** استانداردسازی و تزریق قطعی `aria-label` به آیکون زنگوله مطلع‌سازی موجودی (`icon-notif` / `bakala_product_notify`)، آیکون مقایسه کالا، آیکون ویدیوی گالری محصول و مودال اشتراک‌گذاری.
- **موتور هوشمند برچسب‌گذاری پویا:** افزودن اسکنر خودکار در لایه سرور (SSR) و کلاینت (JS) برای تشخیص هرگونه تگ لینک آیکونی یا بدون متن در تمام قالب‌ها و پلاگین‌ها و الصاق خودکار برچسب معنایی متناسب جهت تضمین نمره ۱۰۰ لایت‌هاوس و خزنده‌های هوش مصنوعی.

## [5.35.0] - 2026-08-14
### رفع سراسری خطای Links must have discernible text در تمام صفحات دسکتاپ و موبایل
- **پوشش کامل نوار دسترسی پایین موبایل (Mobile Bottom Nav):** تزریق برچسب‌های دسترس‌پذیر فارسی (`aria-label` و `title`) برای تمامی ۵ دکمه ناوبری موبایل (صفحه اصلی، جستجوی محصولات، سبد خرید، دسته‌بندی محصولات و حساب کاربری).
- **پوشش دکمه‌های آیکونی هدر و مودال‌ها:** استانداردسازی دکمه تماس/پشتیبانی هدر موبایل (`c-header__faq`)، دکمه مطلع‌سازی موبایل (`#bakala_product_notify`)، دکمه مقایسه کالا در صفحه محصول موبایل و دکمه کش لایت‌اسپید.
- **تست و اعتبارسنجی ۱۰۰٪ صفر خطای لینک خالی:** رسیدن تعداد لینک‌های فاقد برچسب به صفر مطلق در تمامی صفحات محصول و دسته‌بندی دسکتاپ و موبایل بدون کوچک‌ترین تغییر در قالب سایت.

## [5.34.0] - 2026-08-14
### رفع قطعی خطای Elements must only use supported ARIA attributes در دسکتاپ و موبایل بدون تغییر در قالب
- **استانداردسازی W3C APG برای تب‌های ووکامرس (wc-tabs):** اصلاح ساختار تب‌ها، حذف صفت غیرمجاز `aria-selected` از تگ‌های لینک `<a>` و انتقال آن به عنصر دارای نقش معتبر `<li role="tab" aria-selected="true/false">`.
- **حذف صفت نامعتبر aria-expanded از نگهدارنده‌های محتوا:** پاکسازی خودکار `aria-expanded` از تگ‌های `<div>` غیرتعاملی (مانند پلاگین ReadMore) در لایه SSR و کلاینت.
- **تست و اعتبارسنجی ۱۰۰٪ بدون خطا:** پاس شدن قطعی تمام چک‌های دسترسی‌پذیری خزنده‌های هوش مصنوعی (Agentic Browsing) و لایت‌هاوس برای صفحات محصول و دسته‌بندی در دسکتاپ و موبایل.

## [5.33.0] - 2026-08-14
### اضافه شدن قابلیت تنظیم فاصله زمانی دلخواه برای کران‌جاب (Custom Interval Management)
- **تنظیم دقیق فاصله زمانی به دقیقه:** امکان تعیین فاصله زمانی دلخواه برای بازتولید فایل فیزیکی `llms.txt` بر حسب دقیقه (مثلاً هر ۳۰ دقیقه، ۶۰ دقیقه، ۱۲ ساعت، ۲۴ ساعت یا هر عدد دلخواه).
- **دکمه‌های انتخاب سریع (Quick Presets):** کلیدهای میانبر برای انتخاب سریع فاصله‌های متداول (۳۰ دقیقه، ۱ ساعت، ۳ ساعت، ۶ ساعت، ۱۲ ساعت، ۲۴ ساعت/روزانه) به همراه راهنمای محاسباتی تبدیل خودکار دقیقه به ساعت و روز.
- **زمان‌بندی هوشمند در هسته وردپرس:** ثبت اینتروال اختصاصی با فیلتر `cron_schedules` و زمان‌بندی دقیق و سبک رویداد کران بر اساس عدد تنظیمی کاربر.

## [5.32.4] - 2026-08-14
### نمایش زمان نسبی زنده (مثلاً ۵ دقیقه پیش) در پنل وضعیت فایل فیزیکی پیشخوان
- **مانیتورینگ زمان نسبی (Live Human Time Diff):** محاسبه و نمایش هوشمند فاصله زمانی تولید فایل فیزیکی نسبت به لحظه جاری (مانند «۵ دقیقه پیش» یا «۲ ساعت پیش») در کنار تاریخ و ساعت بیلد در پنل مدیریت پیشخوان.

## [5.32.3] - 2026-08-14
### اضافه شدن سوییچ روشن/خاموش نمایش تاریخ در تنظیمات و اصلاح فرمت متنی (Single-Source Clean Timestamp)
- **کلید اختصاصی در پیشخوان:** افزودن سوییچ «درج برچسب زمانی آخرین به‌روزرسانی در فایل (Freshness Signal)» در تنظیمات افزونه جهت کنترل کامل نمایش یا عدم نمایش تاریخ.
- **حذف تاریخ تکراری:** پاکسازی تاریخ از سطر اول انتساب و درج منحصربه‌فرد و تمیز تاریخ و ساعت جلالی/UTC در بلاک‌کوت زیر تیتر H1 تنها در صورت فعال بودن سوییچ.

## [5.32.2] - 2026-08-14
### درج برچسب زمانی استاندارد همگام‌سازی (ISO 8601 & Jalali) در فایل llms.txt جهت ارزیابی تازگی داده توسط هوش مصنوعی
- **سیگنال تازگی محتوا (Data Freshness Signal):** افزودن تاریخ و ساعت دقیق تولید بر اساس فرمت استاندارد بین‌المللی ISO 8601 (`YYYY-MM-DDTHH:MM:SSZ`) همراه با تاریخ جلالی در بلاک‌کوت متادیتا جهت درک بهتر موتورهای هوش مصنوعی (ChatGPT, Perplexity, Claude, Gemini) از به‌روز بودن کاتالوگ محصولات.
- **تضمین ۱۰۰٪ استانداردسازی لایت‌هاوس:** حفظ قرارگیری تیتر اول `# H1` به عنوان اولین هدینگ فایل بدون کوچک‌ترین تداخل در اعتبارسنجی W3C / llmstxt.org.

## [5.32.1] - 2026-08-14
### خودکارسازی کامل همگام‌سازی .htaccess و استریم پرسرعت بدون نیاز به ویرایش دستی هاست (Zero Manual Config)
- **همگام‌سازی اتوماتیک .htaccess:** تزریق و نگهداری خودکار هدرهای CORS در فایل `.htaccess` با تابع استاندارد هسته وردپرس (`insert_with_markers`) در زمان فعال‌سازی و ذخیره تنظیمات بدون نیاز به ویرایش دستی هاست یا نگرانی از تغییرات قالب.
- **استریم فوق‌سریع استاتیک در سطح PHP (Fallback Streamer):** ارسال آنی هدرهای `Access-Control-Allow-Origin: *` و محتوای از پیش کامپایل‌شده روی دیسک با `readfile()` در اولویت ۰ (Priority 0) با زمان پاسخ زیر ۲۰ میلی‌ثانیه، حتی در صورتی که `.htaccess` به هر دلیلی ریست شود.
- **صفر کردن دخالت کاربر:** اجرای تمامی مراحل استاتیک‌سازی، هدرها، کران و CORS به صورت ۱۰۰٪ مستقل و درونی داخل افزونه بایروز.

## [5.32.0] - 2026-08-14
### موتور تولید فایل استاتیک فیزیکی llms.txt با کران‌جاب سرور و رویدادمحور (Zero PHP & TTFB < 20ms)
- **معماری فایل استاتیک فیزیکی:** تولید خودکار فایل فیزیکی `llms.txt` و `llms-full.txt` در روت هاست (`ABSPATH . 'llms.txt'`) با دسترسی مستقیم وب‌سرور لایت‌اسپید بدون لود وردپرس و دیتابیس.
- **کاهش زمان پاسخ (TTFB) به کمتر از ۲۰ میلی‌ثانیه:** حل قطعی و دائمی خطای Timeout در تست‌های Lighthouse، خزنده‌های AI و سیستم‌های ابری.
- **هماهنگی کامل با کران‌جاب سرور پارس‌پک:** اتصال به هوک `brz_cron_generate_static_llms` با امکان زمان‌بندی روزانه، همراه با به‌روزرسانی رویدادمحور هنگام انتشار/ویرایش محصول یا دسته‌بندی و دکمه بازتولید آنی دستی در پنل پیشخوان.
- **پنل وضعیت و مانیتورینگ:** نمایش وضعیت فایل فیزیکی روی دیسک، تاریخ آخرین بیلد، حجم فایل و راهنمای هدرهای CORS در `.htaccess`.

## [5.31.0] - 2026-08-14
### رفع قطعی باگ دو بار نمایش برچسب در کارت‌های موبایل (Double Label Bug Fix)
- **ریشه‌یابی:** در نسخه‌های قبل هم شبه‌عنصر `td::before { content: attr(data-label) }` و هم المان HTML `<span class="buyruz-cell-label">` همزمان نمایش داده می‌شدند و برچسب دو بار چاپ می‌شد.
- **راهکار:** حذف کامل `td::before` در موبایل و اتکای انحصاری به عناصر معنایی HTML `.buyruz-cell-label` و `.buyruz-cell-value` که داخل `.buyruz-cell-inner` با Flexbox عمودی نمایش می‌یابند.
- **نتیجه:** نمایش کامل و روان تمام مشخصات (مخاطب، تمایز کلیدی) در تمامی کارت‌های موبایل بدون حتی یک حرف بریدگی یا تکرار.

## [5.30.0] - 2026-08-14
### بازمهندسی قطعی سیستم کارت‌های موبایل (Hero Card & Zero Clipping) و تقارن اعلای گرید دسکتاپ
- **طراحی کارت قهرمان موبایل (Hero Card Product CRO):** هدر یکپارچه سبز زمردی عمیق (`#05593D`) با عنوان سفید بولد و برچسب طلایی «محصول فعلی»، همراه با کادربندی متمایز و هایلایت نرم تمایزهای کلیدی کالا جهت حداکثر ترغیب به خرید.
- **حل قطعی ۱۰۰٪ مشکل بریدگی متون (Zero Text Clipping):** اصلاح لایه‌بندی عمودی و مستقل فیلدها با پشتیبانی دوطرفه از ساختار جدید DOM و فال‌بک‌های استاتیک، تضمین نمایش کامل و روان متون چندسطری بدون تداخل و خروج از کادر.
- **تفکیک هندسی بی‌نقص ستون‌ها در دسکتاپ:** اعمال صریح خطوط عمودی میان تمامی ستون‌ها در سطر اول و تمامی سطرهای بدنه جدول با `border-collapse: separate`.

## [5.29.0] - 2026-08-14
### بازمهندسی شاهکار ساختار DOM، اصلاح قطعی تفکیک ستون‌های دسکتاپ و سیستم کارت‌های مقایسه موبایل
- **اصلاح قطعی و ۱۰۰٪ تفکیک ستون‌های دسکتاپ:** جایگزینی مدل رندرینگ جدول با `border-collapse: separate` جهت پیشگیری از باگ‌های بلعیده شدن بوردرها توسط موتورهای وب‌کیت/کرومیوم در سلول‌های هایلایت‌شده؛ تضمین نمایش شفاف خط تفکیک میان ستون دوم و سوم در سطر اول و تمامی سطرها.
- **بازسازی بنیادین ساختار DOM (حذف شبه‌عناصر `::before`):** افزودن المان‌های معنایی مستقل HTML (`.buyruz-cell-inner`, `.buyruz-cell-label`, `.buyruz-cell-value`) برای دسترسی‌پذیری کامل، خوانایی بی‌نقص در کارت‌های موبایل و جلوگیری از اختلالات رندرینگ در صفحات کوچک.
- **سیستم کارت‌های مقایسه لوکس موبایل (CRO & Behavioral Psychology 100/100):** نمایش بی‌نقص کارت محصول فعلی همراه با کادر شاخص، برجسته‌سازی تمایزهای رقابتی و نمایش تمام‌عرض و بدون برش متون چندخطی در تمامی کارت‌های محصولات رقیب.

## [5.28.1] - 2026-08-14
### اصلاح تقارن ستونی دسکتاپ، هماهنگی کامل هدر و بهینه‌سازی کارت‌های موبایل
- **تقارن ستونی و مرزبندی مینیمال در دسکتاپ:** اعمال خطوط تفکیک عمودی یکنواخت و ملایم (`1px solid #E2E8F0`) در سراسر ستون‌های هدر (`thead`) و بدنه جدول (`tbody`) و حذف حاشیه‌های سنگین جهت ایجاد گرید منظم، زیبا و چشم‌نواز.
- **یکپارچه‌سازی هدر و عنوان جدول:** هماهنگ‌سازی کامل رنگ و کادربندی سرتیتر سبز زمردی با سطر عنوان ستون‌ها و حذف تضادهای حاشیه‌ای اضافه.
- **بهینه‌سازی لایه‌بندی عمودی کارت‌های موبایل (Stacked Mobile Layout):** نمایش عنوان ویژگی در بالا با فونت ملایم و متن توصیفی در زیر به صورت تمام‌عرض، جهت جلوگیری از فشردگی متون طولانی و خوانایی ۱۰ از ۱۰ در نمایشگرهای کوچک.

## [5.28.0] - 2026-08-14
### بازطراحی و ارتقای جامع UI/UX، سئو، دید هوش مصنوعی (GEO) و دسترسی‌پذیری جدول مقایسه محصولات
- **یکپارچه‌سازی با پالت هویت بصری لوکس بایروز:** بازنویسی کامل استایل‌های جدول با رنگ‌های زمردی عمیق (`#05593D` / `#03432E`)، حاشیه‌های نرم (`#E2E8F0`) و گرادیان شاخص در بنر سرتیتر.
- **رفع قطعی باگ ناخوانایی ردیف اول (محصول فعلی):** اعمال کنتراست بالای ۷:۱ (WCAG AAA) برای متن‌های ردیف اول روی زمینه ملایم سبز و افزودن برچسب شکیل و اختصاصی «محصول فعلی» با نوار شاخص عمودی ۴ پیکسلی در سمت راست.
- **بازمهندسی تجربه موبایل (Mobile Isolated Cards):** تبدیل هوشمند و خودکار ردیف‌های جدول در صفحات کوچک (موبایل) به کارت‌های مستقل و زیبای مقایسه با هدر مجزا و ساختار کلید-مقدار بدون فشرده‌سازی یا شکستگی متن.
- **بهینه‌سازی سئو تکنیکال و موتورهای هوش مصنوعی (SEO & GEO):** افزودن نشانه‌گذاری معنایی `itemscope itemtype="https://schema.org/Table"`، اتصال استاندارد کپشن با `aria-describedby` و بهبود دسترسی‌پذیری پیوندها جهت استخراج حداکثری داده توسط گوگل (Featured Snippets) و مدل‌های هوش مصنوعی (ChatGPT, Gemini, Perplexity, Claude).
- **ارتقای دسترسی‌پذیری (a11y):** حلقه‌های فوکوس واضح (`:focus-visible`) برای ناوبری کیبورد و دکمه‌های لمسی استاندارد با حداقل ارتفاع ۳۸ تا ۴۴ پیکسل.

## [5.27.1] - 2026-08-14
### بازمهندسی ماژول `BRZ_LLMS_Optimizer` به عنوان مکمل تخصصی رنک‌مث (Zero Duplication)
- حذف کامل فیلدها و کدهای موازی با رنک‌مث پرو (مانند انتخاب دسته‌ها، محصولات، برندها و شمارنده‌ها) جهت جلوگیری از تکرار کد و پردازش‌های مضاعف دیتابیس.
- تمرکز انحصاری ماژول بر رفع ۵ نقطه ضعف فنی رنک‌مث: انتقال هوشمند عنوان H1 و متن معرفی به بالاترین خط فایل (H1 Top Placement)، ارسال هدر CORS (`Access-Control-Allow-Origin: *`) برای رفع خطای Fetch، تزریق تگ Discovery در `<head>` صفحات، حذف هدرهای Noindex و تنظیم فرمت استاندارد مارک‌داون.
- بازطراحی صفحه تنظیمات با هماهنگی کامل و هدایت به تنظیمات محتوایی رنک‌مث.

## [5.27.0] - 2026-08-14
### ماژول جدید: بهینه‌ساز دید هوش مصنوعی و مدیریت کامل llms.txt (`BRZ_LLMS_Optimizer`)
- افزودن ماژول مستقل «بهینه‌ساز دید هوش مصنوعی (llms.txt)» با قابلیت روشن/خاموش سراسری در پیشخوان تنظیمات بایروز.
- صفحه اختصاصی تنظیمات تحت منوی «تنظیمات بایروز > بهینه‌ساز llms.txt» با طراحی متریال و ذخیره‌سازی AJAX.
- امکان ویرایش تیتر اصلی H1 و پرامپت هویتی فروشگاه در بالاترین سطر فایل طبق استاندارد رسمی llmstxt.org.
- سوییچ‌های ریز و اختصاصی برای: ارسال هدر CORS (`Access-Control-Allow-Origin: *`) جهت رفع قطعی خطای `Fetch of llms.txt failed` لایت‌هاوس، تزریق تگ کشف (`<link rel="llms-txt">`) در `<head>`، حذف هدرهای مخرب Noindex و اعمال Content-Type استاندارد مارک‌داون.
- کلیدهای تفکیک‌شده برای فعال/غیرفعال‌سازی بخش‌های: نقشه سایت (Sitemaps)، لیست محصولات همراه با تعیین محدودیت تعداد، دسته‌بندی‌ها، برندها، برچسب‌ها و فیلد محتوای تکمیلی و سفارشی.

## [5.26.7] - 2026-08-14
### رفع جامع خطاهای «Links must have discernible text» در صفحات محصول و دسته‌بندی دسکتاپ
- پایش و تحلیل عمیق تمام پیوندهای آیکونی و فاقد متن در صفحات محصول و آرشیو/دسته‌بندی دسکتاپ (`desktop-product.html` و `desktop-product-catetory.html`).
- گسترش فیلتر فوق‌سریع سمت سرور (SSR) در ماژول `BRZ_Product_Desc_Optimizer` برای پوشش تمام دکمه‌های آیکونی شامل: علاقه‌مندی‌ها (`addtowishlist`)، اطلاع‌رسانی موجودی (`icon-notification`)، نمودار قیمت (`icon-statistics`)، مقایسه کالا (`icon-compare`)، اشتراک‌گذاری (`icon-share`)، شبکه‌های اجتماعی فوتر (اینستاگرام، تلگرام، آپارات، توییتر، فیسبوک، پینترست، یوتیوب)، منوی کاربری هدر (`c-header__btn-user`) و دکمه‌های منوی موبایل.
- تضمین ۰ خطای دسترسی‌پذیری پیوندها در تست‌های جامع Lighthouse و Agentic Browsing با حفظ کامل اصالت و دست‌نخورده ماندن کدهای قالب باکالا.

## [5.26.6] - 2026-08-14
### بهینه‌سازی دسترسی‌پذیری سمت سرور (SSR Accessibility & Lighthouse 100%)
- افزودن بافر خروجی پرسرعت و سبک سمت سرور (SSR) به ماژول `BRZ_Product_Desc_Optimizer` جهت تزریق بدون تأخیر صفت‌های `aria-label` به تمام دکمه‌های آیکونی گالری محصول (`addtowishlist`, `icon-notification`, `icon-compare`, `icon-share`).
- حل قطعی خطای `Links must have discernible text` برای ربات‌های لایت‌هاوس و AI Agents حتی در صورت تأخیر اجرای جاوااسکریپت (Perfmatters Delay JS).
- تضمین ۱۰۰ درصدی دست‌نخورده ماندن فایل‌های قالب باکالا و مدیریت جامع بهینه‌سازی‌ها صرفاً از طریق کلید روشن/خاموش در پیشخوان تنظیمات بایروز.

## [5.26.5] - 2026-08-14
### رفع خطاهای دسترسی‌پذیری و ارتقای سازگاری با AI Agents (Accessibility & Lighthouse 100%)
- استانداردسازی کامل سوالات متداول با الگوی رسمی W3C APG Accordion و حذف صفت نامعتبر `role="button"` از تگ‌های هدینگ `<h3>` جهت رفع خطای `ARIA role should be appropriate for the element`.
- ایجاد تگ تعاملی دسترس‌پذیر `<button class="brz-faq-trigger">` درون هدینگ با مدیریت کامل `aria-expanded` و `aria-controls` جهت دستیابی به نمره ۱۰۰ در تست‌های دسترسی‌پذیری و ایجنت‌های هوش مصنوعی.
- رفع خطای `Links must have discernible text` در دکمه‌های آیکونی (علاقه‌مندی‌ها و اشتراک‌گذاری) با تزریق هوشمند و خودکار `aria-label` و `title` در افزونه بایروز و قالب باکالا.

## [5.26.4] - 2026-08-14
### ویژگی جدید و بهینه‌سازی تجربه کاربری (UX & SEO)
- افزودن ماژول «بهینه‌ساز نقد و بررسی دسکتاپ» (`BRZ_Product_Desc_Optimizer`) با امکان روشن/خاموش در پیشخوان تنظیمات بایروز.
- نمایش کامل و بدون کلاپس نقد و بررسی مختصر در صفحات تکی محصول دسکتاپ و حذف دکمه غیرضروری «نمایش بیشتر».
- ارتقای شاخص‌های سئو، تسهیل استخراج چکیده برای موتورهای هوش مصنوعی (GEO) و جلوگیری از پردازش‌های اضافه جاوااسکریپت (Layout Thrashing).
- بهینه‌سازی ۱۰۰ درصدی عملکرد با بارگذاری شرطی و بدون اثر در صورت خاموش بودن ماژول (Zero Footprint).

## [5.26.3] - 2026-08-12
### رفع باگ و همگام‌سازی با قالب باکالا
- تزریق فیلتر یکپارچه‌سازی `option_bakala_options` در هسته اصلی افزونه جهت حل قطعی و بی‌نیاز از ویرایش دستی هشدار `switch_24_Hours_Support` قالب باکالا روی هاست.

## [5.26.2] - 2026-08-12
### رفع باگ تب مشخصات
- رفع قطعی و کامل Notice مربوط به `is_internal_meta_key` برای کلید `_global_unique_id` در تب مشخصات محصول با جایگزینی `get_post_meta` به جای `get_meta`.

## [5.26.1] - 2026-08-12
### رفع باگ و بهینه‌سازی ماژول
- رفع کامل خطای `Call to undefined function wp_get_current_user()` در متد `BRZ_Live_Debugger::init()` با انتقال بررسی سطح دسترسی به زمان لود لایو هندلرها.

## [5.26.0] - 2026-08-12
### ویژگی جدید (New Feature)
- اضافه شدن ماژول رسمیت‌یافته «ردیاب زنده خطاهابی» (`BRZ_Live_Debugger`) به پیشخوان تنظیمات بایروز با قابلیت فعال/غیرفعال‌سازی در پنل ادمین.
- صفر بودن کامل تاثیر سرعت، دیتابیس و هوک‌ها در صورت خاموش بودن ماژول.
- محدودسازی هوشمند کادرهای دیباگ به دسترسی مدیران سایت جهت حفظ امنیت و ظاهر سایت برای کاربران عادی.
- اصلاح هشدار `Undefined array key "switch_24_Hours_Support"` در فایل `single-functions-pc-two.php` قالب باکالا.

## [5.25.14] - 2026-08-12
### رفع قطعی خطای صفحه سفید محصول و هشدار داشبورد
- رفع قطعی و ریشه‌ای خطای `ArgumentCountError` در متد `BRZ_WC_Core_Specs::enrich_rankmath_schema` با اختیاری‌سازی پارامتر `$jsonld = null` و حل کامل مشکل صفحه سفید محصول.
- حذف کامل هشدار `wp_update_plugins` و `wp_update_themes` از داشبورد با بازگردانی پاسخ سالم ۲00 در صورت عدم دسترسی سرور هاست به `api.wordpress.org`.

## [5.25.13] - 2026-08-12
### عیب‌یابی و رفع باگ‌های حیاتی
- رفع کامل خطای `wp_update_plugins` در داشبورد ادمین با افزودن بای‌پاس اختصاصی دامنه‌های سرور به‌روزرسانی وردپرس در ماژول فایروال HTTP.
- جلوگیر ریشه‌ای از اجرای زودهنگام و قطع‌کننده شورت‌کدها در هوک زودهنگام `wp` جهت حل صفحه سفید محصول.
- ارتقای دیباگر به کچ‌کننده عمومی اکسپشن‌ها و تایپ‌ارورهای PHP 8 (`set_exception_handler`).

## [5.25.12] - 2026-08-12
### عیب‌یابی و دیباگ تخصصی
- رفع تداخل مسریابی و ساخت ساختاری فایل زیپ از پوشه فعال سورس‌کد افزونه جهت تضمین آپدیت صحیح نسخه در وردپرس.

## [5.25.2] - 2026-08-12
### عیب‌یابی و دیباگ تخصصی
- بهینه‌سازی ردیاب زنده با پاک‌سازی بافرهای خروجی باز (`ob_end_clean`) جهت تضمین نمایش کادر قرمز رنگ خطاهای متوقف‌کننده روی مرورگر.

## [5.25.1] - 2026-08-12
### عیب‌یابی و دیباگ تخصصی
- افزودن سیستم کچ‌کننده و ردیاب زنده خطاهای متوقف‌کننده (`register_shutdown_function`) در فایل اصلی افزونه جهت استخراج دقیق ریشه خطای صفحه سفید (WSOD) در صفحات محصول بدون حدس و گمان.

## [5.24.3] - 2026-07-24
### تغییر و حذف ماژول
- حذف کامل بخش «پایش متادیتا (متا)» و تمامی کدهای وابسته به آن از پنل عملیاتی و سرویس‌های احراز هویت یکپارچه (SSO).

## [5.24.2] - 2026-07-24
### رفع باگ و بهینه‌سازی SSO
- اصلاح فرآیند احراز هویت یکپارچه (SSO) و جلوگیری ریشه‌ای از ایجاد چرخه ریدایرکت نامحدود (`ERR_TOO_MANY_REDIRECTS`) هنگام ورود به پنل عملیاتی.
- ارسال توکن احراز هویت به عنوان پارامتر کمکی در URL جهت تضمین ورود موفقیت‌آمیز کاربر حتی در صورت محدودیت کوکی‌های Cross-Domain مرورگر.
- افزودن دامنه‌های پنل عملیاتی به فیلتر دامنه‌های مجاز هدایت وردپرس (`allowed_redirect_hosts`).

## [5.24.1] - 2026-07-23
### رفع باگ
- اصلاح کامل نمایش نیم‌فاصله‌ها (`u200c`) در سلول‌ها و متن‌های جدول مقایسه فرانت‌اند و ویرایشگر ادمین با توسعه الگوریتم نرمال‌سازی و اضافه کردن بازه علائم نگارش عمومی یونیکد (ZWNJ / `U+200C`).
- اصلاح نمایش عنوان جدول مقایسه در کادر ویرایشگر پنل مدیریت و تبدیل خودکار کدهای یونیکد هگز (`0627u062a...`) به متون خوانای فارسی.

## [5.23.0] - 2026-07-22
### رفع باگ و بهبود UI/UX
- بازسازی و اصلاح کامل تابع نرمال‌سازی با الگوریتم `mb_chr` جهت بازگردانی متون فارسی یونیکدشده (`u0645...`) به متون روپوش‌نشده و خوانا در عنوان و سلول‌های جدول.
- اصلاح ناترازی و کشیدگی سطرها در ویرایشگر ادمین با تراز فشرده دکمه‌های اکشن (`+`/`-`) در ستون اول.
- افزودن دراپ‌داون پیشرفته انتخاب محصول ووکامرس (Product Picker) در ستون اول جهت انتخاب آسان و بدون نیاز به کپی/پیست شناسه یا لینک.

## [5.22.0] - 2026-07-22
### ویژگی جدید
- افزودن قابلیت لینک‌دار شدن هوشمند محصولات موجود در جدول مقایسه به برگه اختصاصی محصول در تب جدید (`target="_blank"`).
- بررسی خودکار وضعیت انتشار محصول مقصد (`publish`)؛ عدم نمایش لینک تا زمان منتشر شدن محصول در سایت.
- افزودن فیلد مدیریت لینک/شناسه محصول برای هر سطر در پنل ادمین ویرایش محصول ووکامرس.
- پشتیبانی کامل آینده‌نگر از آرایه‌های لینک و شناسه محصول جهت همگام‌سازی از طریق ایجنت‌های هوش مصنوعی و پل آفلاین بایروز شیت.

## [5.21.0] - 2026-07-22
### ویژگی جدید
- تمایز بصری هوشمند و شیک سطر محصول جاری در جدول مقایسه با محصولات مشابه (هایلایت پس‌زمینه ملایم برند + نشان «محصول فعلی»).

## [5.20.1] - 2026-07-22
### رفع باگ
- اصلاح قالب‌بندی و تفکیک فرمت نمایش فیلدهای بازه عددی (حداقل/حداکثر) در تنظیمات مشخصات بایروز.
- پشتیبانی از نقطه‌ویرگول فارسی (؛) و جلوگیری از حذف پسوند فیلدها (مانند «سال» یا «نفر») در حالت‌های مختلف نمایش (فقط حداقل، فقط حداکثر و هر دو مقدار).
- بازسازی و خوداصلاحی هوشمند پسوندها برای داده‌های ذخیره‌شده قبلی در فرانت‌اند و طرح یکپارچه مشخصات.

## [5.20.0] - 2026-07-21
### ویژگی جدید
- افزودن پشتیبانی از تعیین و همگام‌سازی دسته‌بندی اصلی (Primary Category) برای Rank Math Pro در هر دو حالت آنلاین و آفلاین (`OfflineBridge`).

## [5.19.1] - 2026-07-20
### بهبود
- اضافه شدن قابلیت شکست خط خودکار با تگ `<br>` برای مقادیر متنی ساده (Text/String) مشخصات فنی که حاوی ویرگول‌های فارسی (،) یا انگلیسی (,) هستند؛ جهت برطرف شدن و شکستن خط فیلد «سایر محتویات».

## [5.19.0] - 2026-07-20
### بهبود
- تغییر جداکننده خروجی مقادیر چندگزینه‌ای و آرایه‌ای ویژگی‌های متا در فرانت‌اند به تگ شکست خط (<br>) جهت نمایش شکیل‌تر در دسکتاپ و جلوگیری از فشرده شدن متون طولانی در نسخه موبایل.

## [5.18.3] - 2026-07-19
### رفع باگ
- اصلاح رفتار نمایش ویژگی‌های محصول در چیدمان یکپارچه مشخصات (Unified Specs Layout) و احترام به تیک «نمایش در برگه محصول» ووکامرس برای ویژگی‌هایی که مخفی شده‌اند.

## [5.18.2] - 2026-07-15
### ویژگی جدید
- افزودن پشتیبانی کامل از همگام‌سازی ویژگی‌های متایی (`buyruz_product_specs`) در پل آفلاین (`OfflineBridge`).
- اصلاح فرآیند تفکیک و ذخیره‌سازی داده‌های بازه‌ای و آرایه‌ای ویژگی‌های متا در زمان پردازش آفلاین.

## [5.18.1] - 2026-07-15
### بهبود
- حذف فیلتر اصالت کالا (اورجینال/غیراصل) جهت اصلاح تعبیر نادرست قبلی.
- پیاده‌سازی گزینه مستقل «شامل شدن ویژگی‌های محصولات» (`include_attributes`) جهت کنترل حضور یا عدم حضور آنالیز ویژگی‌ها و مشخصات محصول در خروجی گزارش JSON.
- اصلاح نهایی فرم رابط کاربری تنظیمات آنالیزور با چهار گزینه مجزا و تمیز متصل به خروجی دانلود.

## [5.18.0] - 2026-07-15
### ویژگی جدید
- یکپارچه‌سازی رابط کاربری آنالیزور ویژگی‌ها جهت سهولت و وضوح در اعمال تنظیمات روی فایل خروجی.
- افزودن گزینه «شامل شدن لیست محصولات در گزارش» به منظور بهینه‌سازی و کاهش حجم خروجی JSON جهت استفاده ایجنت‌های هوش مصنوعی.

## [5.17.0] - 2026-07-15
### ویژگی جدید
- افزودن پشتیبانی از فیلد نوع «متن ساده (Text)» به سازنده مشخصات فنی محصول (Field Builder)؛ جهت امکان ایجاد فیلدهای متنی دلخواه مانند «سایر محتویات» (`other_components`) بدون درگیر کردن و ایجاد تورم در جدول Taxonomyهای ووکامرس.

## [5.16.21] - 2026-07-14
### بهبود
- ارتقای ساختار داده‌های ساختاریافته فیزیکی با افزودن خودکار ویژگی استاندارد `unitCode` (بر اساس استانداردهای بین‌المللی UN/CEFACT مانند `KGM` برای کیلوگرم و `CMT` برای سانتی‌متر) در کنار `unitText`؛ جهت بهبود بهینه‌سازی خوانش ماشین و تطابق کامل با راهنماهای رسمی گوگل و Schema.org.

## [5.16.20] - 2026-07-14
### رفع باگ
- اصلاح فرآیند تزریق مشخصات فیزیکی (وزن و ابعاد) به اسکیما (Structured Data) با افزودن فیلترهای همزمان ووکامرس (`woocommerce_structured_data_product`) و رنک‌مث پرو (`rank_math/json_ld`) به همراه متد یکپارچه کنترل وضعیت تزریق؛ جهت تضمین و پایداری اعمال متادیتای وزن و ابعاد در نتایج گوگل صرف‌نظر از پلاگین فعال تولیدکننده اسکیما.

## [5.16.19] - 2026-07-14
### پاک‌سازی
- پاک‌سازی و حذف تمامی هک‌ها، لاگرهای تشخیصی موقت و کدهای عیب‌یابی تشخیصی جهت سبک‌سازی افزونه و خروجی نهایی پس از برطرف شدن کامل اختلال ذخیره‌سازی محصول.

## [5.16.18] - 2026-07-14
### بهینه‌سازی
- بهینه‌سازی ذخیره‌سازی مقادیر مشخصات فنی محصول (تنها در صورت تغییر مقدار واقعی) جهت سازگاری کامل با سیستم کش لایت‌اسپید (LiteSpeed Cache) و جلوگیری از حلقه تکرار نامحدود (Infinite Recursion).

## [5.16.17] - 2026-07-14
### رفع خطای هسته
- ایمن‌سازی کامل و خطایابی توابع ثبت و ذخیره‌سازی داده‌های جدول مقایسه و فیلدهای مشخصات فنی با بلاک‌های `try-catch`؛ جهت جلوگیری از اختلال در فرآیند بروزرسانی ووکامرس.

## [5.16.16] - 2026-07-14
### عیب‌یابی
- افزودن نمایش نسخه فعال افزونه در صفحه لاگ تشخیصی جهت صحت‌سنجی اعمال آخرین کدها روی سرور اصلی.

## [5.16.15] - 2026-07-14
### عیب‌یابی
- افزودن مکانیزم نمایش لاگ تشخیصی از طریق پارامتر وب‌سایت جهت دور زدن کش سرور و پیاده‌سازی مانیتورینگ هدرهای Location در زمان خاموشی اسکریپت.

## [5.16.14] - 2026-07-14
### عیب‌یابی
- اصلاح ساختار بازگردانی پشته فراخوانی (Call Stack) در لاگ دیباگ برای نمایش دقیق توابع فراخواننده در انتقال‌های ووکامرس.

## [5.16.13] - 2026-07-14
### عیب‌یابی
- افزودن ابزار تشخیصی پیشرفته موقت جهت مانیتورینگ خطاهای کشنده PHP و آدرس‌های انتقال ووکامرس در سرور اصلی، برای ریشه‌یابی و حل مشکل سفید شدن صفحه در هنگام به‌روزرسانی محصولات.

## [5.16.12] - 2026-07-14
### رفع خطای هسته
- ایمن‌سازی فرآیند همگام‌سازی فیلترهای پیشرفته کناری (`BRZ_Sidebar_Filters::update_lookup_table`) با بلوک‌های `try-catch` و سیستم خودترمیم ساخت جدول پایگاه داده در صورت عدم وجود؛ جهت جلوگیری از خطای کشنده پایگاه داده (Fatal DB Exception) و صفحه سفید در ویرایشگر محصول در PHP 8.1+ هنگام بروز تغییر در مقادیر محصول.

## [5.16.11] - 2026-07-14
### پاک‌سازی
- حذف تمامی لاگ‌های دیباگ و تریس موقت (`error_log`های مربوط به توسعه اولیه) در ماژول `offline-bridge` جهت سبک‌سازی کدهای هسته و جلوگیری از پر شدن بی‌مورد لاگ‌های سرور.

## [5.16.10] - 2026-07-14
### رفع خطای هسته
- ایمن‌سازی فرآیند لاگر تغییرات محصول (`BRZ_Change_Log::insert` و هوک‌های مرتبط) با ساخت خودکار و خودکارساز پویای جدول پایگاه داده در صورت عدم وجود (Self-healing table creation) و پیاده‌سازی بلوک‌های `try-catch`؛ جهت جلوگیری از بروز خطای کشنده (Fatal Exception) و صفحه سفید در ویرایشگر محصول در PHP 8.1+ هنگام بروز تغییر در مقادیر محصول (مانند قیمت یا متادیتاها).

## [5.16.9] - 2026-07-14
### رفع خطای نمایشی
- رفع مشکل خرابی کاراکترهای ضرب در ابعاد (نمایش به صورت `&tiمترes;`) با دیکود کردن کدهای HTML پیش از جایگزینی واحد `m` به `متر`.

## [5.16.8] - 2026-07-14
### بهبود
- ادغام تاگل‌های «وزن» و «ابعاد» در یک تاگل واحد «نمایش وزن و ابعاد» که از فیلتر native ووکامرس `wc_product_enable_dimensions_display` استفاده می‌کند و به‌طور خودکار با قالب باکالا همگام است.
- حذف گزینه «واحد نمایشی وزن» — واحد وزن همیشه از تنظیمات ووکامرس خوانده می‌شود.
- حذف گزینه «لینک استعلام اصالت GS1» و کدهای اضافه مرتبط.
- سؤال SEO: تزریق اسکیما برای وزن و ابعاد از طریق فیلتر `rank_math/snippet/rich_snippet_product_entity` انجام می‌شود که همان روش native ارسال داده به Rank Math Pro است.

## [5.16.7] - 2026-07-14
### بهبود
- ادغام تاگل‌های «وزن» و «ابعاد» در یک تاگل واحد «نمایش وزن و ابعاد» که از فیلتر native ووکامرس `wc_product_enable_dimensions_display` استفاده می‌کند و به‌طور خودکار با قالب باکالا همگام است.
- حذف گزینه «واحد نمایشی وزن» — واحد وزن همیشه از تنظیمات ووکامرس خوانده می‌شود.
- حذف گزینه «لینک استعلام اصالت GS1» و کدهای اضافه مرتبط.
- سؤال SEO: تزریق اسکیما برای وزن و ابعاد از طریق فیلتر `rank_math/snippet/rich_snippet_product_entity` انجام می‌شود که همان روش native ارسال داده به Rank Math Pro است.

## [5.16.6] - 2026-07-14
### اصلاح شده
- بازطراحی ظاهر دکمه‌های تاگل در صفحه تنظیمات WC Core Specs جهت یکپارچگی کامل با سبک بصری دکمه‌های پیشخوان ماژول‌ها (ابعاد `48×28px`، رنگ برند پویا، انیمیشن و سایه یکسان).

## [5.16.5] - 2026-07-14
### اصلاح شده
- بازنویسی و مهندسی مجدد دکمه‌های تاگل به روش حرفه‌ای و بومی بدون نیاز به المان‌های کمکی (Label wrappers و helper Spans) با استایل‌دهی مستقیم به تگ `input[type="checkbox"]` با استفاده از `appearance: none`؛ جهت بهینه‌سازی حجم کدها و حذف صددرصدی هرگونه تداخل یا هاله تیره در پس‌زمینه.

## [5.16.4] - 2026-07-14
### اصلاح شده
- پاک‌سازی کامل استایل‌های چک‌باکس با افزودن ویژگی‌های لغو سراسری سایه (`box-shadow: none !important`) و فیلترهای پس‌زمینه برای دکمه‌های تاگل به منظور حذف قطعی سایه تیره زائد در پس‌زمینه دکمه.

## [5.16.3] - 2026-07-14
### اصلاح شده
- برطرف کردن کامل هاله و سایه خاکستری رنگ (نشت ظاهری چک‌باکس پیش‌فرض سیستم) از زیر دکمه‌های تاگل در مرورگرها با ریست کردن سراسری سودوالمنت‌های :before و :after چک‌باکس و اعمال استیکت مخفی‌سازی بصری.

## [5.16.2] - 2026-07-14
### اصلاح شده
- پاک‌سازی کدهای اضافه تولید بارکد تصویری SVG و تگ‌های تزریق اسکیمای GTIN.
- تغییر ساختار تزریق مشخصات فیزیکی (وزن و ابعاد) به استفاده انحصاری از فیلتر استاندارد رنک‌مث پرو (`rank_math/snippet/rich_snippet_product_entity`) بر اساس مقادیر پویا و استاندارد عددی QuantitativeValue.

## [5.16.1] - 2026-07-14
### اصلاح شده
- برطرف کردن همپوشانی و مشکلات رندر دکمه‌های تاگل (Toggle Switches) در پیشخوان تنظیمات ویژگی‌های هسته‌ای.
- بهینه‌سازی هم‌ترازی و چیدمان ستونی فیلدها در کارت‌های تنظیمات با تفکیک منطقی بخش‌های سوئیچ و فیلدهای متنی.

## [5.16.0] - 2026-07-14
### اضافه شده
- افزودن ماژول جدید «ویژگی‌های هسته‌ای ووکامرس» (wc_core_specs) جهت مدیریت نمایش و سئوی فیلدهای وزن، ابعاد و شناسه جهانی GTIN محصول.
- ارائه پنل پیشرفته تنظیمات با گزینه‌های شخصی‌سازی عنوان نمایشی، نحوه چیدمان ابعاد (یکپارچه در یک ردیف یا تفکیک در ۳ ردیف مجزا) و تبدیل‌های واحد روانشناختی (کیلوگرم به گرم).
- تزریق خودکار و سازگار مقادیر فیزیکی و شناسه‌ها به اسکیمای محصول (JSON-LD) در ووکامرس و رنک‌مث جهت بهبود نتایج جستجو و GEO.
- تولید بارکد تصویری برداری (SVG Code128) کاملاً پویا و تزریق لینک استعلام اصالت GS1 جهت جلب اعتماد حداکثری مشتریان.
- ادغام کامل ماژول جدید با چیدمان و مرتب‌سازی درگ‌ان‌دراپ جدول مشخصات فنی.

## [5.15.1] - 2026-07-14
### اصلاح شده
- همگام‌سازی و یکپارچه‌سازی خودکار شناسه بارکد محصولات (EAN, GTIN, UPC, ISBN) ارسالی از گوگل‌شیت با فیلد نیتیو جدید ووکامرس (`_global_unique_id`) و افزونه رنک‌مث پرو (`_rank_math_gtin_code`) جهت سازگاری و بهبود سئوی اسکیما.

## [5.15.0] - 2026-07-14
### اضافه شده
- افزودن ماژول جدید «برون‌بری مشخصات و ویژگی‌ها» (Specs Exporter) جهت خروجی یکباره ساختار ویژگی‌های متای بایروز، مشخصه‌های ووکامرس و ترم‌های گزینه‌های مربوط به آن‌ها در قالب JSON یکپارچه.
- تعبیه فیلد کپی JSON و دکمه دانلود خودکار فایل در رابط کاربری تنظیمات ماژول در پیشخوان مدیریت بایروز.

## [5.14.4] - 2026-07-13
### بهینه‌سازی شده
- حذف لاگ موقت فایل سیستم در فرآیند تزریق اسکیما جهت بهبود کارایی و سرعت لود صفحات محصول (حذف I/O اضافه روی دیسک در هر بازدید).

## [5.14.3] - 2026-07-13
### اصلاح شده
- رفع مشکل عدم تزریق PropertyValue (ویژگی‌های خودکار و دستی) و همچنین وضعیت محصول (itemCondition) به اسکیمای محصول با اتصال مستقیم به هوک‌های تولید اسکیمای ووکامرس (`woocommerce_structured_data_product`) به عنوان مرجع اصلی و هوک رنک‌مث (`rank_math/json_ld`) به عنوان پشتیبان.

## [5.14.2] - 2026-07-13
### اضافه شده
- افزودن لاگ خطایابی موقت در متد `inject_schema` جهت بررسی علت عدم اعمال ویژگی‌های خودکار روی صفحات محصول فرانت‌اند.

## [5.14.1] - 2026-07-13
### اصلاح شده
- رفع مشکل عدم تزریق PropertyValue به اسکیمای محصول در صفحات فرانت‌اند با ثبت مستقیم فیلتر Rank Math (Timing-independent) جهت جلوگیری از تداخل ناشی از زمان‌بندی هوک `wp`.
- بهبود استخراج شناسه محصول در فیلتر اسکیما با استفاده از `get_queried_object_id` به همراه لایه بک‌آپ `get_the_ID`.

## [5.14.0] - 2026-07-13
### اضافه شده
- افزودن کارت تنظیمات آنالیزور با دو سوئیچ قابل کنترل: فیلتر وضعیت انتشار محصولات (فقط Published یا همه) و بهینه‌سازی حجم خروجی (فقط محصولات دارای نقص).
- افزودن بخش جدید `product_coverage` به خروجی JSON آنالیز ویژگی‌ها شامل ماتریس نواقص ویژگی‌های بحرانی (Critical Specs) به تفکیک محصولات، نرخ پرشدگی هر محصول و آمار کلی پوشش.
- اعمال فیلتر وضعیت انتشار روی تمام کوئری‌های آماری ماژول آنالیز ویژگی‌ها (ووکامرس و بایروز) جهت دقت بیشتر در تحلیل SEO.
- ذخیره‌سازی تنظیمات آنالیزور از طریق AJAX با بررسی دسترسی و nonce.

## [5.13.0] - 2026-07-13
### اضافه شده
- افزودن فیلد و ستون جدید «ارسال به اسکیما» در ماژول آنالیز ویژگی‌ها جهت نمایش وضعیت همگام‌سازی ویژگی‌های سراسری و مشخصات فنی با اسکیمای محصول گوگل (Schema JSON-LD) به صورت یکپارچه.
- ارتقای ساختار خروجی گزارش JSON در API آنالیز ویژگی‌ها با افزودن فیلد `"in_schema"` جهت تسهیل عیب‌یابی و مصرف بهینه داده‌ها توسط ایجنت‌های هوش مصنوعی.

## [5.12.0] - 2026-07-13
### اضافه شده
- افزودن قابلیت تزریق هوشمند و پویای ویژگی‌های ووکامرس (مانند سبک بازی، مکانیک‌ها، تم، طراح و زبان بازی) و فیلدهای مشخصات فنی بایروز (مانند رده سنی، زمان بازی، تعداد بازیکنان و سختی) به بخش `additionalProperty` اسکیمای محصول رنک‌مث.
- ارائه پنل گرافیکی مدیریت به همراه چک‌باکس‌های کنترل در ماژول AI Schema جهت فعال/غیرفعال‌سازی هوشمند فیلدها و حفظ کیفیت چگالی کلمات کلیدی صفحات (SEO/GEO).

## [5.11.12] - 2026-07-13
### اضافه شده
- افزودن قابلیت ثبت لاگ تفصیلی دیتابیس (Log File) در زمان اجرای `rename_slugs_sql` در پل آفلاین جهت خطایابی دقیق علت نادیده گرفته شدن برخی تغییرات اسلاگ‌ها.

## [5.11.11] - 2026-07-13
### اضافه شده
- پیاده‌سازی متد کاملاً پویا و جدید `rename_slugs_sql` در پل آفلاین (Offline Bridge) جهت تغییر نامک ویژگی‌ها و گزینه‌ها به صورت مستقیم از طریق کوئری‌های SQL بدون هاردکد برای دقت میلی‌متری.

## [5.11.10] - 2026-07-13
### اضافه شده
- پشتیبانی از دستور `add_specs` در پل آفلاین (Offline Bridge) جهت فراهم کردن امکان تعریف یا به‌روزرسانی مشخصات فنی به صورت افزایشی (Append/Incremental) بدون آسیب زدن یا پاک کردن مشخصات پیشین.

## [5.11.9] - 2026-07-13
### اضافه شده
- پشتیبانی از تعیین و به‌روزرسانی نامک (Slug) سفارشی ترم‌ها در بخش `create_dependencies` پل آفلاین (Offline Bridge) جهت خودکارسازی کامل همگام‌سازی ویژگی مخاطب هدف.

## [5.11.8] - 2026-07-13
### اضافه شده
- پشتیبانی کامل از نوع فیلد متنی (Text/String) در فیلدهای مشخصات فنی بایروز (مانند فیلد سایر محتویات).
- پیاده‌سازی منطق هوشمند مخاطب هدف (Target Audience Fallback) جهت محاسبه خودکار سنین فیلتر محصول (`filter_min_age` و `filter_max_age`) بر اساس ترم‌های انتخاب‌شده ویژگی سراسری `pa_target-audience` در صورت خالی بودن ورودی‌های دستی.
- اعمال قانون انتخاب چندگانه (Multi-select) برای مخاطبان هدف جهت استخراج پایین‌ترین حد Min و بالاترین حد Max.

## [5.11.7] - 2026-07-12
### اصلاح شده
- رفع ریشه‌ای تکرار مشخصات در تب مشخصات صفحه محصول از طریق انتقال متد حذف هوک پیش‌فرض به هوک `wp` (به جای اجرا در زمان لود زودهنگام افزونه) جهت جلوگیری از تداخل و چاپ مجدد جدول مشخصات توسط قالب.

## [5.11.6] - 2026-07-12
### اصلاح شده
- رفع مشکل چسبیدن پسوندها به اعداد در رندر مشخصات فنی بایروز (مانند اضافه کردن خودکار فاصله قبل از «سال»، «بازیکن» و «میپل»).
- افزودن قابلیت ترجمه خودکار مقادیر بولین «yes» و «no» در ویژگی‌های سراسری باقی‌مانده ووکامرس به تیک سبز و ضربدر قرمز، سازگار با استایل‌های قالب باکالا.

## [5.11.5] - 2026-07-12
### اصلاح شده
- رفع مشکل تکرار مشخصات در صفحه محصول و ایجاد ردیف‌های خالی برای ویژگی‌های سراسری غیرفعال یا حذف‌شده؛ اضافه شدن لایه پیشرفته فیلتر کردن مقادیر خالی و حذف خودکار مشخصات هم‌نام و تکراری (Deduplication) بر اساس برچسب‌های فارسی.

## [5.11.4] - 2026-07-11
### اصلاح شده
- رفع مشکل عدم تفکیک و پارس نادرست قالب‌های فیلدهای بازه عددی (مانند رده سنی و زمان بازی) به دلیل استفاده از نویسه نقطه ویرگول فارسی («؛» با شناسه U+061B) در تنظیمات پیشخوان؛ سیستم اکنون هر دو نقطه ویرگول انگلیسی و فارسی را پشتیبانی می‌کند.

## [5.11.3] - 2026-07-11
### اضافه شده
- پیاده‌سازی قابلیت انتقال هوشمند و خودکار ویژگی‌های ووکامرس (WooCommerce Product Attributes) به مشخصات فنی بایروز (Buyruz Specs) از طریق پی‌لود JSON در پنل پل آفلاین (Offline Bridge).
- بهینه‌سازی پردازش مقادیر ورودی در حین انتقال شامل نرمال‌سازی اعداد فارسی/عربی، استخراج محدوده‌های عددی، تبدیل مقادیر بله/خیر و نگاشت سطوح سختی.

## [5.11.2] - 2026-07-11
### بهبود یافته
- پیاده‌سازی مکانیزم عدم کش شدن (Cache Busting) خودکار کدهای جاوااسکریپت و استایل‌های پل آفلاین با استفاده از زمان تغییر فایل‌ها (`filemtime`) به جای شماره نسخه سراسری افزونه جهت اطمینان از لود آنی کدهای جدید در مرورگر کاربران.

## [5.11.1] - 2026-07-11
### اضافه شده
- افزودن پشتیبانی از دستور `delete_dependencies` در ماژول پل آفلاین (Offline Bridge) جهت فراهم کردن امکان حذف کامل و ایمن ویژگی‌های ووکامرس و تمامی گزینه‌ها (Terms) مربوط به آن‌ها به صورت دسته‌ای از طریق پی‌لود JSON.
- اصلاح اعتبارسنجی سمت کاربر جاوااسکریپت در `offline-bridge.js` جهت پذیرش ساختار پی‌لودهای حذف.

## [5.11.0] - 2026-07-11
### اضافه شده
- توسعه منطق ماژول آنالیز ویژگی‌ها جهت بررسی مشخصات فنی اختصاصی بایروز (Buyruz Dynamic Product Specs) و محاسبه دقیق توزیع مقادیر آن‌ها (شامل بازه‌ها، مقادیر عددی، بولین و مقادیر خارج از ساختار آرایه‌ها).
- افزودن بخش متادیتای راهنما (`metadata`) به خروجی JSON جهت تبیین صریح معماری، نحوه ذخیره‌سازی و روش‌های پاکسازی هر یک از سه نوع ویژگی (ویژگی‌های سراسری ووکامرس، ویژگی‌های محلی محصول و مشخصات فنی بایروز) برای درک آنی توسط ایجنت‌های هوش مصنوعی.
- به‌روزرسانی پنل مدیریت ماژول جهت پایش آماری همزمان مشخصات فنی بایروز در پیشخوان.

## [5.10.0] - 2026-07-11
### اضافه شده
- پیاده‌سازی ماژول آنالیز ویژگی‌ها (Attributes Analyzer) جهت استخراج آمار دقیق استفاده از ویژگی‌های سراسری (Global Attributes) و ویژگی‌های محلی (Custom Local Attributes) در محصولات ووکامرس.
- ارائه خروجی هوشمند و ساختاریافته در قالب JSON جهت سهولت تصمیم‌گیری و استفاده ادمین یا ایجنت‌های هوش مصنوعی.
- ارائه سه روش فراخوانی: درخواست REST API (تحت آدرس `/wp-json/buyruz/v1/attributes-stats` با کلید امنیتی)، دستور خط فرمان WP-CLI (`wp buyruz-attributes-stats`) و دانلود از پنل پیشخوان مدیریت.
- طراحی رابط کاربری مدرن در پیشخوان افزونه جهت پایش سریع ویژگی‌های بی‌استفاده و پرکاربرد.

## [5.9.8] - 2026-07-11
### اضافه شده
- توسعه منطق فرستنده پل آفلاین (Offline Bridge) جهت پشتیبانی کامل از همگام‌سازی و ورود بسته‌های داده چیدمان جدول مشخصات (`layout`).
- پشتیبانی از بارگذاری و رندر خودکار کدهای تاکسونومی برند دلخواه (مانند `pwb-brand` یا `yith_product_brand`) و بقیه تاکسونومی‌های دلخواه خارج از ویژگی‌های استاندارد ووکامرس در جدول مشخصات محصول جهت رفع تکرار داده‌ها.

## [5.9.7] - 2026-07-11
### بهبود یافته
- پیاده‌سازی قابلیت بستن پنجره پاپ‌آپ تنظیمات بازه با کلیک روی فضای بیرونی (Overlay Background Click) بدون انجام عملیات ذخیره داده جهت افزایش راحتی کاربری.

## [5.9.6] - 2026-07-11
### بهبود یافته
- بازطراحی تعاملی فیلدهای پیشوند و پسوند (Prefix/Suffix) به عنوان فعال‌کننده‌ها و محرک‌های اصلی (Modal Triggers) پاپ‌آپ تنظیمات بازه جهت تطبیق کامل با مدل ذهنی کاربر.
- غیرفعال‌سازی ورودی گزینه‌ها (Options) برای فیلدهای بازه‌ای و استفاده از آن صرفاً جهت ذخیره‌سازی داده‌های الگو در پشت صحنه.

## [5.9.5] - 2026-07-11
### اصلاح شده
- رفع باگ عدم رندر فیلدهای فرمت بازه و دکمه حذف به دلیل تداخل توابع افزونه‌ای وردپرس در لودر HTML.
- بهینه‌سازی کدهای استایل CSS قالب تنظیمات و رفع توابع نامعتبر استایل Tab Navigation.

## [5.9.4] - 2026-07-11
### بهبود یافته
- بهینه‌سازی کامل رابط کاربری سازنده مشخصات فنی (Field Builder) با غیرفعال‌سازی هوشمند فیلدهای تکی پیشوند و پسوند برای رکوردهای از نوع بازه عددی (Range) و ممانعت از تداخل و تناقض‌های ورودی در پنل کاربری.
- طراحی استایل دکمه‌ای برای کادر فرمت بازه جهت ترغیب بصری کاربر به کلیک و تنظیم مقادیر از طریق پنجره پاپ‌آپ.

## [5.9.3] - 2026-07-11
### بهبود یافته
- راست‌چین‌سازی (RTL) قالب چیدمان ورودی‌های پنجره پاپ‌آپ تنظیم بازه جهت انطباق کامل با رسم‌الخط فارسی (پیشوند در سمت راست و پسوند در سمت چپ).

## [5.9.2] - 2026-07-11
### اضافه شده
- پیاده‌سازی رابط کاربری هوشمند و ویژوال (Range Format Modal Builder) در پنل سازنده مشخصات فنی: ادمین سایت دیگر نیاز به تایپ دستی تگ‌ها و کدهای الگوی بازه ندارد و با کلیک روی فیلد گزینه‌ها، یک پنجره پاپ‌آپ شکیل جهت تعیین مجزای پیشوند، پسوند و متن بینابینی برای هر یک از ۳ حالت (فقط کمینه، فقط بیشینه و هر دو مقدار) در اختیار خواهد داشت.
- بهبود سیستم پردازش الگوهای متنی بازه (Template Parser) جهت جلوگیری از تکرار چندباره پیشوندها و پسوندها و همگام‌سازی پیش‌فرض‌ها بر اساس مقادیر وارد شده.

## [5.9.1] - 2026-07-11
### اصلاح شده
- محدود کردن دامنه تاثیر منطق انطباق سناریوی ویژگی سن (Audience Fallback) منحصراً به فیلترهای سایدبار (جدول انطباق و نتایج فیلترینگ) و حذف آن از جدول فیلدهای مشخصات فنی در صفحه محصول (تک محصول) جهت ممانعت از نمایش اطلاعات متناقض.

## [5.9.0] - 2026-07-11
### اضافه شده
- پیاده‌سازی فیلترینگ بازه‌ای شکیل و پیشرفته در سایدبار آرشیو محصولات با پشتیبانی کامل از ۵ حالت نمایش و تعامل برای فیلدهای بازه عددی (Range Specs):
  - اسلایدر محدوده دو زبانه (Dual Range Slider) برای تعیین کمینه و بیشینه دقیق به همراه مسیر رنگی فعال.
  - ارزش تک عددی / انطباق هوشمند (Smart Overlap) به همراه دکمه ریست اختصاصی جهت جستجوی سناریوهای ویژه مانند سن مناسب (مثال: جستجوی سن ۱۰ سال که بازی‌های با بازه ۳ تا ۱۲ سال را برمی‌گرداند).
  - دکمه‌های انتخاب سریع (Preset Chips) با پشتیبانی از فرمت‌های منعطف Label:Min-Max برای تسهیل خرید روی موبایل.
  - منوهای کشویی زوجی «از» و «تا» (Dropdowns) با همگام‌سازی AJAX.
  - فیلدهای عددی تایپ‌کردنی «از» و «تا» (Numeric Inputs).
- افزودن قابلیت گام تعاملی (Filter Step) برای اسلایدرها و دراپ‌دان‌ها در سطح تنظیمات ویجت.
- پیاده‌سازی سیستم هوشمند انطباق ثانویه (Audience Age Fallback) به صورت کاملاً خودکار و بدون هاردکد: در صورتی که مشخصه سن برای محصولی ثبت نشده باشد، سیستم به طور خودکار بازه سنی را از روی ترم‌های ویژگی ووکامرس «مخاطب هدف» (یا رده سنی) استخراج کرده و در جدول فیلترها و خروجی محصول اعمال می‌کند.

## [5.8.2] - 2026-07-11
### بهبود یافته
- اصلاح اصطلاحات متنی پرامپت کپی‌شونده با جایگزینی واژه «باکس کپی‌شونده» به جای «کادر کد مارک‌داون» برای درک صریح‌تر هوش مصنوعی

## [5.8.1] - 2026-07-11
### بهبود یافته
- بهینه‌سازی و بازنویسی دستورالعمل متنی کپی‌شونده (Prompt Engineering) برای هوش مصنوعی جهت وادار کردن ایجنت به ارسال کلیدهای مرتب‌شده در قالب کادر کد مارک‌داون (Copyable Markdown Code Block) حاوی شناسه‌های تمیز خط‌به‌خط

## [5.8.0] - 2026-07-11
### اضافه شده
- پیاده‌سازی سیستم ناوبری تب‌بندی شده (Tabbed Navigation) در صفحه تنظیمات مشخصات برای جداسازی «سازنده مشخصات فنی (Field Builder)» از «چیدمان یکپارچه (Unified Layout)» جهت رفع مشکل اسکرول طولانی صفحه
- پیاده‌سازی روند مرتب‌سازی هوشمند مبتنی بر هوش مصنوعی (AI-Assisted Layout Manager) با قابلیت کپی مستقیم لیست به کلیپ‌بورد با یک کلیک جهت ارسال به ایجنت
- پیاده‌سازی پردازشگر متنی هوشمند سمت کلاینت (Resilient Parser) برای تحلیل خروجی کپی شده از چت ایجنت، استخراج خودکار شناسه‌های معتبر (Slugs) و نادیده گرفتن کدهای اضافی/توضیحات متنی در زمان پیست چیدمان
- بازطراحی بخش چیدمان‌های اختصاصی دسته‌بندی‌ها با مکانیزم سوئیچر یکپارچه (Unified Dropdown Selector) جهت جلوگیری از بزرگ و شلوغ شدن صفحه و اعمال ارث‌بری خودکار از چیدمان عمومی

### حذف شده
- حذف کامل کتابخانه درگ اند دراپ jQuery UI Sortable و استایل‌های سنگین مربوطه جهت بهینه‌سازی بارگذاری و کاهش حجم خروجی کلاینت

## [5.7.3] - 2026-07-11
### رفع شده
- غیرفعال‌سازی درایور درگ بومی مرورگر (`draggable="true"`) جهت ممانعت از تداخل با موتور مرتب‌سازی ووکامرس و رفع افکت تاری/شبح (Ghost Drag) کدر در زمان کشیدن و رها کردن کارت‌ها

## [5.7.2] - 2026-07-11
### بهبود یافته
- طراحی مجدد و ارتقای رابط کاربری (UI/UX) پنل چیدمان یکپارچه مشخصات به صورت کاملاً مدرن و در سطح محصولات تراز اول SaaS
- افزودن رنگ‌بندی تفکیک‌شده و منحصر‌به‌فرد برای نشان‌ها (Badges) بر اساس نوع مشخصه (مشخصه‌های بایروز، ویژگی‌های ووکامرس، وزن و ابعاد) جهت افزایش خوانایی و اسکن سریع اطلاعات
- اعمال افکت‌های سایه نرم (Elevation Shadow) و چرخش پویا روی کارت‌های در حال درگ جهت ایجاد بازخورد بصری طبیعی و لذت‌بخش در کاربری کشیدن و رها کردن
- پیاده‌سازی انیمیشن ضربان‌دار و نرم (Pulse Animation) برای پیش‌نمایش فضای خالی (Placeholder) در حین جابجایی کارت‌ها

## [5.7.1] - 2026-07-11
### اضافه شده
- پیاده‌سازی سیستم «چیدمان یکپارچه و ترکیبی مشخصات صفحه محصول (Unified Specs Layout)» جهت چیدمان یکپارچه مشخصه‌های فنی بایروز، ویژگی‌های ووکامرس (Attributes) و مقادیر فیزیکی (وزن/ابعاد) در یک لیست واحد
- پیاده‌سازی صفحه مدیریت درگ اند دراپ با استفاده از کتابخانه بومی وردپرس `jQuery UI Sortable` با انیمیشن‌های نرم، حالت پیش‌نمایش (Placeholder) و سازگاری کامل با مرورگرهای مختلف
- پشتیبانی از «الگوهای اختصاصی دسته‌بندی (Category Overrides)» جهت تعریف چیدمان‌های مجزا و سفارشی برای هر دسته‌بندی خاص محصول با قابلیت ارث‌بری خودکار از آخرین چیدمان عمومی

### بهینه‌سازی
- غیرفعال‌سازی هوشمند رندرر بومی مشخصات ووکامرس خارج از فاز فایر شدن هوک جهت جلوگیری از خطاهای احتمالی یا کرش در ساختار مدیریت هوک‌های PHP 8.x

## [5.7.0] - 2026-07-09
### اضافه شده
- ماژول جدید «فیلتر سایدبار آرشیو» (Sidebar Filters Module) جهت پیاده‌سازی سیستم فیلترینگ هوشمند، فوق‌سریع و داینامیک در صفحات آرشیو محصولات و دسته بندی‌ها
- پیاده‌سازی معماری «جدول جستجوی سفارشی (Custom Lookup Table)» با نام `wp_buyruz_filters_lookup` همراه با ساختار کاملاً پویا و ایندکس‌های عددی و متنی جهت تضمین سرعت اجرای کوئری‌ها زیر ۰.۰۱ ثانیه در مقیاس‌های بالا
- پیاده‌سازی منطق همگام‌سازی بلادرنگ محصولات با استفاده از هوک‌های ذخیره‌سازی، بروزرسانی و حذف ووکامرس/وردپرس
- پیاده‌سازی منطق تغییر کوئری‌های فرانت‌اند ووکامرس از طریق هوک `posts_clauses` جهت اتصال و اعمال شروط روی ستون‌های عددی و متنی جدول جستجوی جدید
- پیاده‌سازی ویجت کلاسیک و کاملاً تعاملی «فیلتر پیشرفته بایروز (Specs)» جهت قرارگیری بومی در سایدبار قالب‌های کلاسیک (مانند باکالا) با رندر هوشمند انواع فیلدها (رنج، آرایه، بولین، عددی)
- پیاده‌سازی اسلایدرهای رنج دوتایی (Dual Range Sliders) سبک و کاملاً سفارشی در محیط فرانت‌اند با استایل مدرن و تولتیپ‌های پویا منطبق بر جهت‌گیری راست‌چین (RTL)
- پیاده‌سازی سیستم فیلترینگ با AJAX مدرن با استفاده از Vanilla JS (Fetch API) برای استخراج و جایگزینی محتوای محصولات بدون بارگذاری مجدد کل صفحه
- سازگاری کامل با دکمه‌های ناوبری مرورگر (History API / window.history.pushState / popstate) جهت حفظ تاریخچه فیلترها و امکان به اشتراک‌گذاری لینک‌های فیلتر شده
- افزودن قابلیت Debounce در ارسال درخواست‌های کلاینت در زمان تغییر مقادیر اسلایدرها و ورودی‌ها جهت بهینه‌سازی منابع سرور
- افزودن پنل تنظیمات پیشرفته در پیشخوان مدیریت جهت شخصی‌سازی سلکتورهای قالب (سلکتور نگهدارنده محصولات، صفحه‌بندی و تعداد نتایج) و ابزار بازسازی بسته‌ای کل دیتابیس (Chunked AJAX Rebuild) برای محصولات موجود

## [5.6.0] - 2026-07-09
### اضافه شده
- تفکیک نوع داده عددی به دو نوع داده مجزا: عدد صحیح (integer) و عدد اعشاری (decimal) جهت کنترل ورودی و اعتبارسنجی مستقل
- قابلیت تعریف الگوهای سفارشی برای بازه (Format range templates) در ستون گزینه‌های فیلد بازه به صورت ۱۰۰٪ عمومی و داینامیک بدون هیچ‌گونه متن هاردکد شده در منطق افزونه

## [5.5.3] - 2026-07-09
### اضافه شده
- پیاده‌سازی منطق هوشمند ادغام و نمایش فیلدهای زوج (حداقل/حداکثر مانند حداقل سن و حداکثر سن) در قالب یکپارچه ردیف واحد در خروجی فرانت‌اند (Single Product tab) با پاک‌سازی خودکار برچسب‌ها و ترکیب پسوند/پیشوندها

## [5.5.2] - 2026-07-09
### رفع شده
- اصلاح اعتبارسنجی کلاینت (جاوا اسکریپت) در پل آفلاین جهت به رسمیت شناختن ساختار شیء تنظیمات `create_specs` و عبور بدون خطای «آرایه خالی یا نامعتبر»

## [5.5.1] - 2026-07-09
### رفع شده
- حذف کادر تایید هشدار مرورگر (confirm) هنگام حذف فیلدها در سازنده فیلد جهت تسریع عملیات حذف فیلدها
- همگام‌سازی بلادرنگ و همزمان مقادیر فلگ‌های حذف متاباکس در صفحه ویرایش محصول به صورت کاملاً سینکرونوس جهت جلوگیری از بازگشت فیلدهای حذف شده در زمان بروزرسانی‌های سریع و مکرر

## [5.5.0] - 2026-07-09
### اضافه شده
- افزودن قابلیت `create_specs` به ورودی پل آفلاین (Offline Bridge) جهت فراهم کردن امکان تعریف، مقداردهی و کانفیگ فیلدهای مشخصات فنی به همراه تایپ‌ها، برچسب‌ها، پسوند و پیشوندها از طریق کدهای JSON به صورت یکپارچه

## [5.4.9] - 2026-07-09
### بهبود یافته
- پاک‌سازی کامل تمام کلیدهای سخت‌کد شده و فیلدهای پیش‌فرض مربوط به رومیزی/اسباب‌بازی از منطق اجرای افزونه
- تبدیل فیلدساز مشخصات به یک ابزار کاملاً عمومی (Generic) و خام برای هر نوع فروشگاهی (از لباس تا بازی فکری) بدون داشتن هیچ‌گونه بذر از پیش تعریف‌شده
- داینامیک‌سازی کامل سیستم نگاشت کلیدهای پل آفلاین بدون وابستگی به کلیدهای خاص

## [5.4.8] - 2026-07-09
### اضافه شده
- نگاشت خودکار کلیدهای بدون پیشوند مشخصات فنی در ماژول پل آفلاین (Offline Bridge) جهت عدم نیاز به درج دستی پیشوند _brz_spec_ در گوگل شیت و بسته‌های JSON
- افزودن قابلیت تبدیل خودکار و درلحظه اعداد فارسی و عربی به انگلیسی در ورودی‌های عددی متاباکس صفحه محصول جهت بهبود تجربه کاربری

## [5.4.7] - 2026-07-09
### اضافه شده
- انطباق کامل کلیدهای مشخصات فنی افزونه با نیازمندی‌های درخواستی و معماری Google Sheets
- پشتیبانی از نگاشت داینامیک فیلدهای بازه (manual_age, players, time) به کلیدهای متا مستقل (manual_min_age/manual_max_age, min_players/max_players, min_time/max_time)
- پشتیبانی از اعداد اعشاری (Decimal) برای مشخصه سختی بازی (difficulty)
- تولید و ذخیره خودکار کلیدهای کمینه/بیشینه فیلتر رده سنی (filter_min_age و filter_max_age) در پس‌زمینه هنگام ذخیره رده سنی دستی

## [5.4.6] - 2026-07-09
### رفع شده
- حل مشکل سازگاری پی‌اچ‌پی ۸.۳ و وردپرس (خطای تعداد آرگومان‌های intval در زمان اجرای فیلترهای sanitize_meta) با استفاده از تابع واسط اختصاصی

## [5.4.5] - 2026-07-09
### رفع شده
- موقت: افزودن بلوک try-catch و خروجی خطا با تابع wp_die در زمان ذخیره محصول جهت عیب‌یابی خطای صفحه سفید (WSOD)

## [5.4.4] - 2026-07-09
### بهبود یافته
- بهبود ساختاری و بصری رابط کاربری متاباکس مشخصات فنی محصول (UI/UX)
- انتقال ابزار «افزودن مشخصه جدید» به بالای متاباکس جهت دسترسی سریع‌تر و تجربه کاربری بهتر
- طراحی مجدد دکمه حذف به صورت دکمه مجزای متنی با افکت هاور قرمز رنگ جهت وضوح بیشتر
- افزودن حالت پیام خالی (Empty State) اختصاصی در زمان عدم انتخاب هیچ فیلدی برای راهنمایی کاربر

## [5.4.3] - 2026-07-09
### بهبود یافته
- بازنویسی کامل متاباکس صفحه محصول جهت عدم نمایش فیلدهای بدون مقدار به صورت پیش‌فرض
- افزودن قابلیت انتخاب و اضافه کردن پویای مشخصات مورد نیاز به هر محصول با استفاده از لیست کشویی تعاملی
- پاک‌سازی و حذف کامل متادیتاها از دیتابیس در صورت غیرفعال‌سازی یا خالی گذاشتن مقادیر آن‌ها در زمان بروزرسانی محصول

## [5.4.2] - 2026-07-09
### اضافه شده
- قابلیت ثبت خودکار گزینه‌های جدید در مشخصات فنی آرایه‌ای (مانند تعداد بازیکن) هنگام دریافت اطلاعات از REST API یا افزونه پل آفلاین (Offline Bridge) بدون نیاز به تعریف دستی در پیشخوان ادمین
- افزودن قابلیت ثبت سریع گزینه‌های جدید به صورت مستقیم در متاباکس صفحه ویرایش محصول

## [5.4.1] - 2026-07-09
### رفع شده
- اصلاح و حل ریشه‌ای باگ تکرار آیکون فلش (Chevron) در تمام دراپ‌داون‌های (منوهای سلکت) تنظیمات افزونه با تغییر استایل پس‌زمینه از حالت shorthand به background-color جهت حفظ استایل بومی و بدون تداخل وردپرس

## [5.4.0] - 2026-07-09
### اضافه شده
- ماژول جدید «مشخصات فنی محصول» (Product Specs Manager) با سازنده فیلد پویا (Dynamic Field Builder) در بک‌اند
- پشتیبانی از فیلدهای متنوع (Boolean، Number، Range و Array/Multi-select) در متاباکس اختصاصی و مدرن صفحه محصول
- شکستن خودکار بازه‌های عددی به دو فیلد کمینه و بیشینه در دیتابیس جهت بهینه‌سازی حداکثری فیلترینگ‌های آینده
- ثبت خودکار فیلدهای داینامیک در REST API ووکامرس برای دسترسی امن و مستقیم از پلتفرم‌های خارجی
- نمایش هوشمند مشخصات فنی سفارشی با ساختار HTML منطبق بر قالب باکالا (bakala) دقیقاً پیش از ویژگی‌های پیش‌فرض ووکامرس

## [5.3.4] - 2026-07-03
### بهبود یافته
- اجباری کردن فلگ Secure در هدر Set-Cookie پورتال SSO جهت دور زدن مشکلات ناشی از تداخلات SSL پروکسی‌ها/کلودفلر
- به کارگیری ارسال دستی هدر Set-Cookie به جای تابع setcookie برای تضمین تنظیم شدن صد درصدی کوکی روی مرورگرهای مدرن

## [5.3.3] - 2026-07-03
### بهبود یافته
- پاکسازی خودکار و هوشمند دامنه کوکی (Cookie Domain) از نقطه‌های اضافی اول و آخر جهت جلوگیری از خطای پذیرش کوکی در مرورگر
- جلوگیری هوشمند از ایجاد لوپ ریدایرکت (ERR_TOO_MANY_REDIRECTS) با متوقف کردن ریدایرکت خودکار در صورت وجود کوکی نامعتبر و نمایش خطای شفاف راهنما

## [5.3.2] - 2026-07-03
### اضافه شده
- پیاده‌سازی مکانیزم احراز هویت خودکار (Silent SSO) به صورت کاملاً بی‌صدا و بدون نیاز به کلیک روی دکمه
- افزودن پل هدایت ورود هوشمند (Login Redirect Bridge) جهت همگام‌سازی نشست ورود پنل با سایت اصلی و پشتیبانی کامل از ورودهای موبایل/پیامکی (OTP)

## [5.3.1] - 2026-07-03
### رفع شده
- اصلاح استایل و هماهنگی بخش تب‌های مدیریت پورتال SSO با ظاهر پیش‌فرض افزونه وردپرس
- رفع مشکل نمایش کاربران تکراری در جدول مدیریت دسترسی‌ها
- برطرف کردن مشکل غیرفعال (disabled) بودن و کلیک‌نشدن چک‌باکس‌های دسترسی برای کاربران مدیر کل
- افزودن قابلیت جستجو و اضافه کردن دستی کاربران با هر نقشی به لیست مدیریت دسترسی
- تغییر پیش‌فرض دسترسی کاربران غیرمدیر به غیرفعال (تیک‌نخورده)
- حذف ستون اضافی پایش متادیتا (متا) به دلیل میزبانی استاتیک آن روی گیت‌هاب و فعال‌سازی پیش‌فرض دسترسی آن برای تمام اعضای پنل

## [5.3.0] - 2026-07-03
### اضافه شده
- ماژول پورتال احراز هویت یکپارچه (SSO) جهت اتصال کاربران به پنل عملیاتی متمرکز
- ثبت لاگ فعالیت‌های کاربران در دیتابیس وردپرس و نمایش آن در پیشخوان مدیریت افزونه
- نقاط دسترسی REST API جدید برای بررسی دسترسی و پردازش امن پل ارتباطی آفلاین

### تغییر یافته
- متد همگام‌سازی پل ارتباطی آفلاین برای امکان فراخوانی مستقیم از بیرون پیشخوان وردپرس

## [5.2.6] - 2026-07-01
### رفع شده
- placeholder و fallback مقدار shared_data_dir از `/static-data` به `/home/buyruz/static/data` اصلاح شد

## [5.2.5] - 2026-07-01
### رفع شده
- باگ اساسی: `generate_pending_only()` آدرس صفحات رو از `raw selected_pages` میخوند که URL نداشت
- Fix: اول `build_map_data()` اجرا میشه (URL resolve میکنه)، بعد pending_urls از نتیجه ساخته میشه
- تعداد صفحات پردازش‌شده حالا دقیق ثبت میشه (از count واقعی، نه مقدار قبلی)

## [5.2.4] - 2026-07-01
### رفع شده
- مسیر shared_data_dir و urls-map.json به `/home/buyruz/static/data/` تغییر یافت (ادغام با پوشه static)
- مهاجرت خودکار مسیرهای قدیمی (`/static-data/`, `/home/buyruz/static-data/`)

## [5.2.3] - 2026-07-01
### رفع شده
- بازسازی دستی urls-map.json حالا مستقیم اجرا میشه (بدون debounce/schedule)
- افزایش BATCH_LIMIT از ۱۰۰ به ۵۰۰ — دیگه برای سایت‌هایی با کمتر از ۵۰۰ صفحه batched نمیره

## [5.2.0] - 2026-06-30
### رفع شده
- دکمه "همگام‌سازی سایت‌مپ" حالا مستقیم import می‌کنه بدون نیاز به confirm
- فیکس container reference برای loading state بعد از حذف تب سایت‌مپ

### حذف شده
- تب "صفحات سایت‌مپ" حذف شد (صفحات سایت‌مپ کاملاً خودکار هستن و نیاز به انتخاب دستی ندارن)

## [5.1.0] - 2026-06-30
### رفع شده
- همگام‌سازی خودکار سایت‌مپ: وقتی آدرس سایت‌مپ ذخیره بشه، خودکار fetch و import انجام میشه
- ساخت خودکار urls-map.json بعد از هر ایمپورت موفق (بدون نیاز به کلیک دستی)
- اولین بار که ماژول با سایت‌مپ تنظیم‌شده لود بشه، خودکار sync شروع میشه

### اضافه شده
- متد `handle_auto_import()` برای اجرای خودکار fetch → import → regeneration از طریق WP-Cron
- فایل `.gitattributes` برای حذف پوشه `.git` از ZIP توزیع

## [5.0.0] - 2025-07-14
### اضافه شده
- سیستم ارتباطی جدید (Communication_Interface) بین کامپوننت‌ها
- نسخه‌گذاری قرارداد (Contract Versioning) در URLs_Map
- خواندن و نمایش وضعیت تأیید دریافت (Acknowledgment) در پنل ادمین
- تشخیص نوع صفحه از URL prefix هنگام ایمپورت سایت‌مپ
- عدم ایمپورت مجدد صفحات حذف‌شده توسط اپراتور
- فیلد content_hash (SHA-256) برای تشخیص تغییرات محتوا
- تنظیم shared_data_dir از پنل ادمین
